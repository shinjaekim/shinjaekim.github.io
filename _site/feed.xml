<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.2.2">Jekyll</generator><link href="http://localhost:4000/feed.xml" rel="self" type="application/atom+xml" /><link href="http://localhost:4000/" rel="alternate" type="text/html" /><updated>2022-10-16T13:08:07+09:00</updated><id>http://localhost:4000/feed.xml</id><title type="html">KSJ Archive</title><subtitle>archive of my backend path </subtitle><author><name>Shinjae Kim</name><email>lo97729@gmail.com</email></author><entry><title type="html">Section 4 인증/보안 - 쿠키, 세션</title><link href="http://localhost:4000/%EC%BD%94%EB%93%9C%EC%8A%A4%ED%85%8C%EC%9D%B4%EC%B8%A0/cookie-session/" rel="alternate" type="text/html" title="Section 4 인증/보안 - 쿠키, 세션" /><published>2022-09-21T00:00:00+09:00</published><updated>2022-09-21T00:00:00+09:00</updated><id>http://localhost:4000/%EC%BD%94%EB%93%9C%EC%8A%A4%ED%85%8C%EC%9D%B4%EC%B8%A0/cookie%20session</id><content type="html" xml:base="http://localhost:4000/%EC%BD%94%EB%93%9C%EC%8A%A4%ED%85%8C%EC%9D%B4%EC%B8%A0/cookie-session/"><![CDATA[<p>[week9]</p>
<h2 id="간단정리">간단정리</h2>
<ul>
  <li></li>
</ul>

<p><br /></p>

<h2 id="느낀점">느낀점</h2>

<p><br /></p>

<h2 id="스스로-답해보기">스스로 답해보기</h2>
<ul>
  <li>cookie를 설명해봐</li>
  <li>session은 무엇이니</li>
</ul>

<p><br /></p>

<h2 id="정리">정리</h2>
<h3 id="1-들어가며">1. 들어가며</h3>

<p>http message의 특징에 대해 생각해보고 가자</p>

<blockquote>
  <p>stateless</p>
</blockquote>

<p>무상태성이 특징이다. 즉, 클라이언트가 서버에 request를 하고 response를 받으면 연결은 끝이다.</p>

<p><strong><em>그럼 여기에 보안인증 개념을 더해서 생각해보자</em></strong></p>

<p>stateless + 인증이라.. 딱 이 개념까지만 생각하면, 우리는 매번 정보를 요청할때마다 로그인 인증을 계속 해야 할 것이다.</p>

<p>근데 실제로는 그렇지 않다.</p>

<hr />

<h3 id="2-쿠키">2. 쿠키</h3>

<blockquote>
  <p>서버가 클라이언트(브라우저,로컬)에 데이터를 저장한다.</p>
</blockquote>

<p>이렇게 되면 로그인 이후 생성한 쿠키를 클라이언트에 저장함으로써, 이후 사용자가 정보를 서버에 요청할때는 쿠키를 참고하여 stateful한 상태를 유지할 수 있게 된다.</p>

<p>-&gt; response header에 set-cookie를 설정한 후 쿠키 전송</p>

<h4 id="2-1-쿠키-옵션">2-1 쿠키 옵션</h4>

<p>쿠키의 구성요소는 다음과 같다</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>키
값
유효시간
도메인
경로
</code></pre></div></div>

<p>이와 관련된 옵션들을 살펴보자.</p>

<p>1.도메인 
쿠키의 구성요소에 도메인이 있다면, 쿠기를 전송하고 받아오는 작업은 <strong><em>쿠키에 명시된 도메인과 서버의 도메인이 일치할때</em></strong>만 가능하다.</p>

<p>2.Path
URL상 포트번호 다음 / 이후의 uri들을 path라고 하는데, 이 <strong><em>path정보들을 모두 포함하는 경우에만</em></strong> 쿠키 작업이 가능하다.</p>

<p>3.MaxAge or Expires
유효시간이 없다면 <strong>세션쿠키</strong>이다. 이 경우 브라우저가 종료되면 쿠키는 사용 불가하다</p>

<p>MaxAge는 쿠키가 몇초동안 유지될지, Expires는 어떤 시간,날짜까지 유지할지를 결정한다. 이 경우 <strong>영속성쿠키</strong>로써 브라우저가 종료되도 유지된다.</p>

<p>4.Secure
HTTPS의 경우에만 쿠키를 전송한다.</p>

<p>5.HttpOnly
기본값은 false인데, true일 경우 자바스크립트를 통한 쿠키접근은 불가하다.</p>

<p>6.SameSite
Lax / Strict / None의 옵션이 있다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Lax :Cross-Origin 요청이면 'GET' 메소드에 대해서만 쿠키를 전송할 수 있습니다.
Strict : Cross-Origin이 아닌 same-site 인 경우에만 쿠키를 전송 할 수 있습니다.
None: 항상 쿠키를 보내줄 수 있습니다. 다만 쿠키 옵션 중 Secure 옵션이 필요합니다.
</code></pre></div></div>

<p>if)</p>

<ul>
  <li>
    <p>http://www.mynmae-domain.com/ —&gt; http://www.mynmae-domain.com = ***Same-Origin-</p>
  </li>
  <li>
    <p>http://www.mynmae-domain.com —&gt; http://www.yournmae-domain.com = ***Cross-Origin-</p>
  </li>
</ul>

<hr />

<h3 id="3-세션">3. 세션</h3>

<p>사용자가 인증에 성공한 상태라면 그 상태를 <strong><em>세션</em></strong> 이라고 한다.
이 후 서버는 세션의 id를 반환하고, 이를 쿠키에 담아서 클라이언트에게 전달한다.
그러면 로그인 상태가 유지되어서 (쿠키로부터 세션id 일치 여부만 확인하면 되니까 stateful한 상태를 유지할 수 있다.</p>

<blockquote>
  <p>쿠키랑 차이점은 뭔데??</p>
</blockquote>

<p>위 설명만 보면 결국 쿠키를 이용한다. 따라서 차이가 없어 보이기도 하지만 차이점은 분명 존재한다.</p>

<p>바로 <strong><em>‘사용자 정보 파일을 어디에 저장하는가’</em></strong> 이다.</p>

<p>때문에 보안측면에서 세션방식이 더 유리하다고 말할 수 있겠다. 하지만 그만큼 사용자가 몰리면 서버에는 과부하가 올 수도 있겠다.</p>]]></content><author><name>Shinjae Kim</name><email>lo97729@gmail.com</email></author><category term="코드스테이츠" /><category term="Security" /><category term="네트워크" /><summary type="html"><![CDATA[[week9] 간단정리]]></summary></entry><entry><title type="html">Section 4 인증/보안 기초</title><link href="http://localhost:4000/%EC%BD%94%EB%93%9C%EC%8A%A4%ED%85%8C%EC%9D%B4%EC%B8%A0/security-basic/" rel="alternate" type="text/html" title="Section 4 인증/보안 기초" /><published>2022-09-20T00:00:00+09:00</published><updated>2022-09-20T00:00:00+09:00</updated><id>http://localhost:4000/%EC%BD%94%EB%93%9C%EC%8A%A4%ED%85%8C%EC%9D%B4%EC%B8%A0/security%20basic</id><content type="html" xml:base="http://localhost:4000/%EC%BD%94%EB%93%9C%EC%8A%A4%ED%85%8C%EC%9D%B4%EC%B8%A0/security-basic/"><![CDATA[<p>[week9]</p>
<h2 id="간단정리">간단정리</h2>
<ul>
  <li></li>
</ul>

<p><br /></p>

<h2 id="느낀점">느낀점</h2>

<p><br /></p>

<h2 id="스스로-답해보기">스스로 답해보기</h2>
<ul>
  <li>Https란 무엇인가?</li>
  <li>hashing, salting</li>
  <li>인증, 권한부여(인가)</li>
  <li>3티어 아키텍쳐 흐름</li>
</ul>

<p><br /></p>

<h2 id="정리">정리</h2>
<h3 id="1-들어가며">1. 들어가며</h3>

<p>과거의 기억을 떠올려보면 네트워크상에서 정보를 전송하고 전달받기 위한 http는 여러 통신 프로토콜 중의 하나였다. 이를 잘 표현한 문장이 바로 떠오른다. 바로 “http는 하나의 배송사와 같은 것이다.” 정보를 전달하는 배송사.. 현실에서 배송사마다 그들만의 특징들이 있듯이 http또한 특징이 있다.</p>

<ul>
  <li>특징들
    <ul>
      <li>무상태성</li>
      <li>request body, response body</li>
    </ul>
  </li>
</ul>

<p>지금까지 학습해온 spring framework를 통해 웹 어플리케이션을 만드는 것은, csr방식인 이상(ssr도 마찬가지이겠지만 특히나 더), 클라이언트와의 정보 공유라는 활동이 있어야 존재 의미를 가질 것이다.</p>

<p>여기서 정보공유를 http message를 통해 하게 되는데 (클라이언트가 메시지를 보내면 컨트롤러의 각 handler method로 연결된다.), 생각해보면 중간자가 개입하여 그 메시지를 탈취하면 중요한 정보들을 도둑맞게 될 가능성이 높다.</p>

<blockquote>
  <p>그래서 https가 존재한다.</p>
</blockquote>

<hr />

<h3 id="2-https">2. https</h3>

<p>https는 Hyper Text Transfer Protocol Secure Socket Layer의 약자이다. 길게 말할 것 없이 http프로토콜에 보안이 추가된 것이다.</p>

<p>보안이라는 기능은 어떻게 추가될까?? -&gt; https 특징을 들여다보며 알아보자.</p>

<h4 id="2-1-암호화-비대칭키--대칭키">2-1 암호화 (비대칭키 / 대칭키)</h4>

<p>먼저 비대칭키와 대칭키부터 확실히 잡고가자.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>비대칭키 - 비밀키로 암호화 / 복호화
대칭키 - 비밀키와 공개키로 암호화 / 복호화
</code></pre></div></div>

<p>these are the encryption(암호화) method of Http</p>

<p>that there are two ways means that there is trade off of advantage. 
Asymmetric Key(비대칭키) is more complicated than Symmetric key(대칭키). So if we do all transfer process with Asymmetric key, it costs more.</p>

<p>there is a way of relieving it.
easily thinking of transfering process, data transfering is more frequent.</p>

<p>so,</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1. when transfer data each other, use Symmetric key
2. when trasnfer Symmetric key each other, use Asymmetric key
</code></pre></div></div>

<h4 id="2-2-인증서">2-2 인증서</h4>

<p>cetificate has the information of domain. so if client get the response form server, they can compare the domain of server itself to domain information included in certificate.</p>

<p>if middle man interrupt it, it cause change of domain information, so client can notice that there was intteruption by comparing certificate domain to it.</p>

<h4 id="2-3-ca">2-3 CA</h4>

<p>it refers to the authorized agencies that issue certificates. it’s roll is encypting the server’s public key and information with CA’s private key.</p>

<p>after client get the certificate, client can decrypt it with CA’s public key. if it is impoosible, it means Certificate is not issued by CA.</p>

<hr />

<h3 id="3-hashing">3. Hashing</h3>

<p>인증수단으로서 가장 먼저 떠오르는 것이 비밀번호이다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>비밀번호가 없다면??

아이디만으로 서버에 정보 요청이 가능
아이디를 다른사람이 아는 순간 누구나 정보 요청이 가능
</code></pre></div></div>

<p>근데 중간에 껴들어서 비밀번호마저 탈취한다면???</p>

<blockquote>
  <p>그래서 암호화 - 해싱을 이용한다</p>
</blockquote>

<p>해싱 - 문자열을 임의의 다른 문자열로 변환</p>

<h4 id="3-1-이메일만-존재할때">3-1 이메일만 존재할때</h4>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1. client가 email과 관련된 정보 요청
2. server에서 email과 관련된 정보 제공
</code></pre></div></div>

<h4 id="3-2-이메일--비밀번호">3-2 이메일 + 비밀번호</h4>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1. client가 email과 관련된 정보 요청
2. server에서 db에 저장된 암호화 일치하는지 여부 확인
3. 일치한다면 그때 정보 제공
</code></pre></div></div>

<h4 id="3-2-이메일--암호--해싱">3-2 이메일 + (암호 + 해싱)</h4>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1. client가 email과 관련된 정보 요청
2. server에서 password hashing
3. db에 저장된 hashing된 암호와 일치하는지 여부 확인
4. 일치한다면 그때 정보 제공
</code></pre></div></div>

<p>즉 server 내부의 hashing 알고리즘을 알지 못한다면 http messeage에서 정보를 탈취했다고 하더라도, 비밀번호를 알아낼 수 없음.</p>

<h4 id="hashing의-철칙">hashing의 철칙</h4>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1. 복호화는 오래걸려도 암호화는 빠르게
    암호화가 느리면 매번 요청할때마다 긴 시간이 걸릴 것

2. 최대한 해시값의 중복은 피한다
    이를 구현하는 알고리즘들이 존재

3. 조금만 문자가 바뀌어도 엄청나게 바뀌는 해시값
    나비효과
</code></pre></div></div>

<hr />

<h3 id="4-salt">4. salt</h3>

<p>조미료가 첨가된 음식은 더 맛있어진다.</p>

<p>한편, 조미료가 첨가된 음식을 만드는 사람과 먹는 사람 입장에서 생각해보자.</p>

<p>만드는 사람은 당연히 자기가 만드는 음식에 무엇이 들어갔는지 알고 있다. 하지만.. 먹는 사람은 백종원급이 아닌 이상은 어떤 조미료가 들어갔는지 정확히 알지 못한다.</p>

<p>이 예를 그대로 적용하면 된다.</p>

<p>앞서 학습한 해싱의 경우 비밀번호를 암호화 하였다.</p>

<p>여기에 소금(salt)를 더하여 더 맛있는 비밀번호를 만들어보자. 단, 만드는 사람 이외의 사람들은 어떤 소금이 얼마나 들어간건지는 모른다.</p>

<h4 id="4-1-salt-추가시의-흐름">4-1 salt 추가시의 흐름</h4>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1. client가 email과 관련된 정보를 요청한다.
2. server는 db에서 salt와 hashing된 값을 가져온다.
3. server가 email과 함께 받은 password를 salt한 후 hashing한다.
4. 비교 후 일치하면 정보를 제공한다.
</code></pre></div></div>

<h4 id="4-2-소금을-첨가하는-방법">4-2 소금을 첨가하는 방법</h4>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1 salt는 패스워드 별로 유일한 값을 가진다.

2. 비밀번호 변경시 새로운 salt를 사용한다

3. 재사용은 안된다

4. salt는 db에 같이 저장된다.
</code></pre></div></div>]]></content><author><name>Shinjae Kim</name><email>lo97729@gmail.com</email></author><category term="코드스테이츠" /><category term="Security" /><category term="네트워크" /><summary type="html"><![CDATA[[week9] 간단정리]]></summary></entry><entry><title type="html">Section 3 Spring - 트랜잭션</title><link href="http://localhost:4000/%EC%BD%94%EB%93%9C%EC%8A%A4%ED%85%8C%EC%9D%B4%EC%B8%A0/%ED%8A%B8%EB%9E%9C%EC%9E%AD%EC%85%98/" rel="alternate" type="text/html" title="Section 3 Spring - 트랜잭션" /><published>2022-09-05T00:00:00+09:00</published><updated>2022-09-05T00:00:00+09:00</updated><id>http://localhost:4000/%EC%BD%94%EB%93%9C%EC%8A%A4%ED%85%8C%EC%9D%B4%EC%B8%A0/%ED%8A%B8%EB%9E%9C%EC%9E%AD%EC%85%98</id><content type="html" xml:base="http://localhost:4000/%EC%BD%94%EB%93%9C%EC%8A%A4%ED%85%8C%EC%9D%B4%EC%B8%A0/%ED%8A%B8%EB%9E%9C%EC%9E%AD%EC%85%98/"><![CDATA[<p>[week12]</p>
<h2 id="간단정리">간단정리</h2>
<ul>
  <li>선언형 방식으로 구현 가능하다</li>
  <li>클래스와 메서드 레벨에 붙은 애너테이션은 메서드 레벨이 우위이다.</li>
  <li>전파레벨과 격리레벨을 설정할 수 있다.</li>
  <li>AOP를 적용하여 코드에서 완전히 분리할 수 있다.</li>
</ul>

<p><br /></p>

<h2 id="느낀점">느낀점</h2>
<p>이론상 이해는 가장 쉬운데, 적용할때 이게 맞나?? 내가 놓친게 있지 않나 싶은 생각이 많이 든다. 물론 다른 챕터도 마찬가지이긴 하지만.. 특히나 더..</p>

<p><br /></p>

<h2 id="스스로-답해보기">스스로 답해보기</h2>
<ul>
  <li>트랜잭션이 뭐지?</li>
  <li>트랜잭션의 경계는 무슨 뜻일까?</li>
</ul>

<p><br /></p>

<h2 id="정리---들어가며">정리 - 들어가며</h2>

<p>필수기능들을 계층별로 나누면 지금까지 공부한대로 API, SERVICE, DATA의 세가지 계층으로 나뉠 것이다.</p>

<p>그리고 이 모든 계층을 관통하는 기능들이 있을 것인데,</p>

<p>AOP의 적용 대상으로, 트랜잭션, 보안이 있다.</p>

<p>이번엔 트랜잭션을 중점으로 들어가보자.</p>

<hr />

<h3 id="1-트랜잭션">1. 트랜잭션</h3>

<blockquote>
  <p>A는 인터넷으로 맥북을 샀다. 그런데 도중 오류로 결제가 이루어지지 않았고, 다시 주문하려다가 마음이 바뀐 A는 다시 주문하는 것을 포기했다. 그런데 3일뒤 맥북이 도착했다??</p>
</blockquote>

<p>위 경우는 트랜잭션이 보장되지 않았기 때문에 일어난 행운이다. (판매자 입장에선 참사)</p>

<p>분석해보면 위의 사례는 크게 두가지의 쪼갤 수 없는 행동으로 구성되어있다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1. 결제  2.배송
</code></pre></div></div>

<p>하지만 생각해보자. 물리적으로는 두가지로 쪼개지지만 하나만 독단적으로 일어나면 어처구니없는 상황 아닌가??</p>

<p>즉 물리적으로는 나뉘어있지만 논리적으로는 하나의 작업으로 인식되어야하는 작업들인 것이다.</p>

<p>이것을 트랜잭션이라고 하며, 트랜잭션 하의 작업들은 모두 성공하거나 모두 실패해야하는 공동 운명체이다.</p>

<p>이 외에도 여러가지의 특징이 있는데 이를 <strong><em>ACID</em></strong>라고 한다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1. A - Atomicity(원자성)
    모두 성공하거나 모두 실패하거나

2. C - Consistency(일관성)
    짜여진대로(비즈니스 로직대로) 일관되게 수행되었는가

3. I - Isolation(격리성)
    두개의 트랜잭션이 있다면 서로 영향을 끼치면 안된다.

4. D - Durability(지속성)
    데이터베이스의 특징으로, 종료되어도 지속적으로 유지되어야 한다.
</code></pre></div></div>

<h3 id="1-2-롤백-커밋">1-2 롤백, 커밋</h3>

<blockquote>
  <p>앞선 A의 사례가 정상적으로 돌아가려면 어떤 일이 있어야 했을까??</p>
</blockquote>

<p>1) 만약 결제와 배송이 성공적으로 진행되었다면, 이에 수반한 결과가 데이터에 정상적으로 변경되어 저장되어야 하며,</p>

<p>2) 하나라도 실패했다면, 모든 데이터들이 원래대로 돌아가야 할 것이다.</p>

<p>1번을 커밋, 2번을 롤백이라고 한다.</p>

<p>커밋을 하게되면 변경 결과가 데이터베이스에 영구 저장되며, 롤백을 하면 작업 직전 상태로 되돌아간다.</p>

<hr />

<h3 id="2-스프링에서의-트랜잭션-구현">2. 스프링에서의 트랜잭션 구현</h3>

<ol>
  <li>코드베이스에서 구현한다.</li>
</ol>

<p>영속성컨텍스트를 학습할때 보았던 EntityManagerFactory를 이용하면 EntityManager 인스턴스를 생성할 수 있는데, 이렇게 생성한 인스턴스로부터 트랜잭션 인스턴스를 생성할 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="kd">private</span> <span class="nc">EntityManager</span> <span class="n">em</span><span class="o">;</span>
    <span class="kd">private</span> <span class="nc">EntityTransaction</span> <span class="n">tx</span><span class="o">;</span>

    <span class="nd">@Bean</span>
    <span class="kd">public</span> <span class="nc">CommandLineRunner</span> <span class="nf">testJpaBasicRunner</span><span class="o">(</span><span class="nc">EntityManagerFactory</span> <span class="n">emFactory</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">em</span> <span class="o">=</span> <span class="n">emFactory</span><span class="o">.</span><span class="na">createEntityManager</span><span class="o">();</span>
        
				<span class="c1">// (1)</span>
				<span class="k">this</span><span class="o">.</span><span class="na">tx</span> <span class="o">=</span> <span class="n">em</span><span class="o">.</span><span class="na">getTransaction</span><span class="o">();</span>

        <span class="k">return</span> <span class="n">args</span> <span class="o">-&gt;</span> <span class="o">{</span>
            <span class="n">tx</span><span class="o">.</span><span class="na">begin</span><span class="o">();</span>
            <span class="o">.</span>
            <span class="o">.</span>
            <span class="o">.</span>
            <span class="n">em</span><span class="o">.</span><span class="na">persist</span><span class="o">(</span><span class="n">member</span><span class="o">);</span>
					
            <span class="n">tx</span><span class="o">.</span><span class="na">commit</span><span class="o">();</span>    <span class="c1">// (2)</span>
        <span class="o">};</span>
    <span class="o">}</span>

</code></pre></div></div>
<p>이렇게 하는 것이 코드베이스이지 않나 싶다.. (찾아보는 중)</p>

<p>단점으로는 당연히 필수기능과 부가기능의 코드들이 섞여서 가독성이 악화되며 유지보수의 어려움을 낳는다는 것이 되겠다.</p>

<ol>
  <li>선언형으로 구현한다.</li>
</ol>

<p>클래스나 메서드 레벨에 @Transactional 을 추가해주기만 하면 되며, 둘 다 적용된 경우 메서드 레벨의 애너테이션이 우선적용된다.</p>

<ol>
  <li>AOP방식으로 구현한다.</li>
</ol>

<p>선언형으로 작성하는 애너테이션마저 꼴보기 싫다면 AOP방식으로 적용 가능하다.
(추후 추가..)</p>

<h3 id="2-1-선언형에서의-여러-설정">2-1 선언형에서의 여러 설정</h3>

<ol>
  <li>
    <p>Transaction Propagation</p>

    <p>Propagation.REQUIRED
     ‘트랜잭션 내놔’
     진행중인 트랜잭션에 참여한다. 없다면 새로 트랜잭션을 시작하고 참여한다.</p>

    <p>Propagation.REQUIRES_NEW
     ‘무조건 새거’
     트랜잭션을 하나 새로 만들어 진행한다. 이때, 진행중인 트랜잭션이 있다면 잠시 멈추게된다.</p>

    <p>Propagation.MANDATORY
     ‘트랜잭션 내놔, 근데 내가 안만들어’
     진행중인 트랜잭션에 참여한다. 없다면 예외를 발생시킨다.</p>

    <p>Propagation.<em>NOT_SUPPORTED</em>
     ‘난 혼자한다’
     트랜잭션은 필요없다. 해당 메서드가 실행될때는 진행중이던 트랜잭션은 잠시 멈춘다.</p>

    <p>Propagation.<em>NEVER</em>
     ‘트랜잭션 만들지마라’
     트랜잭션은 필요없다. 진행중인 트랜잭션이 있으면 예외를 발생시킨다.</p>
  </li>
  <li>
    <p>Isolation Level</p>

    <p>Isolation.DEFAULT
     기본 값</p>

    <p>Isolation.READ_UNCOMMITTED
     다른 트랜잭션에서 커밋하지 않은 데이터를 읽는 것을 허용</p>

    <p>Isolation.READ_COMMITTED
     다른 트랜잭션에 의해 커밋된 데이터를 읽는 것을 허용</p>

    <p>Isolation.REPEATABLE_READ
     트랜잭션 내에서 한 번 조회한 데이터를 반복해서 조회해도 같은 데이터가 조회</p>

    <p>Isolation.SERIALIZABLE
     동일한 데이터에 대해서 동시에 두 개 이상의 트랜잭션이 수행되지 못하도록</p>
  </li>
</ol>]]></content><author><name>Shinjae Kim</name><email>lo97729@gmail.com</email></author><category term="코드스테이츠" /><category term="Spring" /><summary type="html"><![CDATA[[week12] 간단정리 선언형 방식으로 구현 가능하다 클래스와 메서드 레벨에 붙은 애너테이션은 메서드 레벨이 우위이다. 전파레벨과 격리레벨을 설정할 수 있다. AOP를 적용하여 코드에서 완전히 분리할 수 있다.]]></summary></entry><entry><title type="html">Section 3 Spring - JPA</title><link href="http://localhost:4000/%EC%BD%94%EB%93%9C%EC%8A%A4%ED%85%8C%EC%9D%B4%EC%B8%A0/Section3-JPA/" rel="alternate" type="text/html" title="Section 3 Spring - JPA" /><published>2022-09-01T00:00:00+09:00</published><updated>2022-09-01T00:00:00+09:00</updated><id>http://localhost:4000/%EC%BD%94%EB%93%9C%EC%8A%A4%ED%85%8C%EC%9D%B4%EC%B8%A0/Section3%20JPA</id><content type="html" xml:base="http://localhost:4000/%EC%BD%94%EB%93%9C%EC%8A%A4%ED%85%8C%EC%9D%B4%EC%B8%A0/Section3-JPA/"><![CDATA[<p>[week11]</p>
<h2 id="간단정리">간단정리</h2>
<ul>
  <li>spring data jdbc는 단방향, spring data jpa는 양방향</li>
  <li>다대일을 먼저 매핑하고 일대다는 필요할때 매핑하자</li>
  <li>@Entity와 @Id는 꼭 붙이자.</li>
</ul>

<p><br /></p>

<h2 id="느낀점">느낀점</h2>
<p>JDBC를 보고 JPA를 보니, 솔직히 spring data jpa가 더 어렵다고 자료에 적혀있긴 했지만, 쌩초보 입장에서는 뭐가 어려운건지는 분간이 안된다. 아무래도 더 깊게 사용하지 않아서 그런거겠지..</p>

<p><br /></p>

<h2 id="스스로-답해보기">스스로 답해보기</h2>
<ul>
  <li>jdbc api와 jpa api</li>
  <li>spring data JDBC 와 spring data JPA</li>
</ul>

<p><br /></p>

<h2 id="정리---들어가며">정리 - 들어가며</h2>

<p>jdbc가 코드수준에서 데이터베이스를 사용할 수 있게 해주는 표준 사양이었다면</p>

<blockquote>
  <p>JPA는 ORM 기술의 표준 사양이다.</p>
</blockquote>

<p>즉 구현체는 따로 있는데 그것이 <strong><em>Hibernate ORM</em></strong>이다</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>JPA
Hibernate ORM
JDBC API
</code></pre></div></div>

<p>이렇게 보면 되겠다.</p>

<p>JPA를 풀어쓰면 Java Persistence API가 되는데,</p>

<p>JPA를 이용하여 데이터베이스에 접근하게 되는 것은</p>

<p>1) 영속성 컨텍스트의 1차캐시에 엔티티를 저장한다
2) 데이터베이스에 반영한다.</p>

<p>의 순서가 되겠다.</p>

<p>이 기술을 사용하기 위해서는 다음의 의존 라이브러리를 추가한다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>implementation 'org.springframework.boot:spring-boot-starter-data-jpa'
</code></pre></div></div>

<p>또한 다음의 설정을 application.yml에 추가하자</p>
<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">//application.yml</span>

<span class="nl">jpa:</span>
    <span class="nl">hibernate:</span>
      <span class="n">ddl</span><span class="o">-</span><span class="nl">auto:</span> <span class="n">create</span>  <span class="err">#</span> <span class="o">(</span><span class="mi">1</span><span class="o">)</span> <span class="n">스키마</span> <span class="n">자동</span> <span class="n">생성</span>
    <span class="n">show</span><span class="o">-</span><span class="nl">sql:</span> <span class="kc">true</span>      <span class="err">#</span> <span class="o">(</span><span class="mi">2</span><span class="o">)</span> <span class="no">SQL</span> <span class="n">쿼리</span> <span class="n">출력</span>

</code></pre></div></div>

<h3 id="1-연간관계-매핑">1. 연간관계 매핑</h3>

<p>spring data JDBC를 떠올리고 넘어가보자.</p>

<ol>
  <li>테이블과 엔티티를 매핑 할 때
    <ul>
      <li>기본기카 될 멤버에 @Id</li>
      <li>table의 이름이 entity와 다르다면 클래스 레벨에 @table(“이름”)</li>
    </ul>
  </li>
  <li>엔티티간의 연관관계를 매핑 할 때</li>
</ol>

<p>애그리거트를 기준으로 세가지의 규칙이 있었다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1) 도메인은 애그리거트의 루트로 접근한다.
2) 동일 애그리거트는 객체로 접근한다.
3) 애그리거트 루트간에는
    
    1:1 , 1:N 은 외래키로 참조한다
        AggregateReference&lt;&gt;
    
    N:N은 두가지 다 쓴다.
        1:N , N:1 의 관계를 만들어주는 객체를 만들고,
        같은 애그리거트 안에서는 객체 참조
        다른 애그리거트는 N에 1의 id값을 멤버로 넣어준다.
</code></pre></div></div>

<p>그리고 이 기준들은 <strong><em>단방향 매핑</em></strong>이었음을 알아두자.</p>

<p>JPA는 어떻게 다른지 보자.</p>

<ol>
  <li>테이블과 엔티티를 매핑 할 때
    <ul>
      <li>클래스 레벨에 @Entity
  (name = “ “) 로 변경 가능하다.</li>
      <li>기본키에 @Id
        <ul>
          <li>@Id(strategy = GenerationType.IDENTITY or SEQUENCE or TABLE)</li>
        </ul>
      </li>
      <li>그 외 컬럼을 위한 것
        <ul>
          <li>@Column - nullabe, updatable, unique등의 애트리뷰트 지정 가능</li>
          <li>@Enumerated(value = EnumType.STRING)</li>
        </ul>
      </li>
      <li>@Table
  (name = “ “) 로 변경 가능하다.</li>
    </ul>
  </li>
  <li>
    <p>엔티티간의 연관관계를 매핑 할 때</p>

    <p>1) 1:N
     @OneToMany(mappedBy = “N에 추가한 멤버의 이름”)
     1의 멤버에 N을 리스트로 추가하거나  -&gt; 일대다는 잘 사용하지 않음 / 다대일을 하고 필요할때 사용
     @ManyToOne
     @JoinColumn(name = “테이블에 저장될 이름”)
     N의 멤버에 1을 객체로 추가한다. -&gt; 우선 사용</p>

    <p>2) N:N
     중간에 1:N, N:1을 만들어주는 클래스를 설계하고,
     1)을 그대로 적용</p>

    <p>3) 1:1
     무엇이 상위도메인인지 파악하자.</p>
  </li>
</ol>]]></content><author><name>Shinjae Kim</name><email>lo97729@gmail.com</email></author><category term="코드스테이츠" /><category term="Spring" /><summary type="html"><![CDATA[[week11] 간단정리 spring data jdbc는 단방향, spring data jpa는 양방향 다대일을 먼저 매핑하고 일대다는 필요할때 매핑하자 @Entity와 @Id는 꼭 붙이자.]]></summary></entry><entry><title type="html">[개인] PatchDto에 관하여</title><link href="http://localhost:4000/%EA%B0%9C%EC%9D%B8%ED%83%90%EA%B5%AC/PatchDto%EC%97%90-%EA%B4%80%ED%95%98%EC%97%AC/" rel="alternate" type="text/html" title="[개인] PatchDto에 관하여" /><published>2022-08-27T00:00:00+09:00</published><updated>2022-08-27T00:00:00+09:00</updated><id>http://localhost:4000/%EA%B0%9C%EC%9D%B8%ED%83%90%EA%B5%AC/PatchDto%EC%97%90%20%EA%B4%80%ED%95%98%EC%97%AC</id><content type="html" xml:base="http://localhost:4000/%EA%B0%9C%EC%9D%B8%ED%83%90%EA%B5%AC/PatchDto%EC%97%90-%EA%B4%80%ED%95%98%EC%97%AC/"><![CDATA[<h2 id="이-글에-담긴-것">이 글에 담긴 것</h2>
<ul>
  <li>patchDto의 필드변수 설정</li>
</ul>

<h2 id="공부하던-부분">공부하던 부분</h2>
<ul>
  <li>API계층 Dto 만들기</li>
</ul>

<h2 id="의문점">의문점</h2>
<ol>
  <li>patchdto의 필드변수는 어떤 것들이 들어가야 하는가??</li>
</ol>

<h2 id="정리">정리</h2>

<p>먼저 DTO는 왜 만드는가를 생각해보아야 한다. 예제를 통해 알아보자.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@PostMapping</span>
    <span class="kd">public</span> <span class="nc">ResponseEntity</span> <span class="nf">postGate</span><span class="o">(</span><span class="nd">@RequestParam</span><span class="o">(</span><span class="s">"number"</span><span class="o">)</span> <span class="kt">int</span> <span class="n">number</span><span class="o">,</span>
                                   <span class="nd">@RequestParam</span><span class="o">(</span><span class="s">"rentStatus"</span><span class="o">)</span> <span class="kt">boolean</span> <span class="n">rentStatus</span><span class="o">,</span>
                                   <span class="nd">@RequestParam</span><span class="o">(</span><span class="s">"price"</span><span class="o">)</span> <span class="kt">int</span> <span class="n">price</span><span class="o">){</span>

        <span class="nc">Map</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">,</span> <span class="nc">Object</span><span class="o">&gt;</span> <span class="n">map</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">HashMap</span><span class="o">&lt;&gt;();</span>

        <span class="n">map</span><span class="o">.</span><span class="na">put</span><span class="o">(</span><span class="s">"number"</span><span class="o">,</span> <span class="n">number</span><span class="o">);</span>
        <span class="n">map</span><span class="o">.</span><span class="na">put</span><span class="o">(</span><span class="s">"rented"</span><span class="o">,</span> <span class="n">rentStatus</span><span class="o">);</span>
        <span class="n">map</span><span class="o">.</span><span class="na">put</span><span class="o">(</span><span class="s">"price"</span><span class="o">,</span> <span class="n">price</span><span class="o">);</span>

        <span class="k">return</span> <span class="k">new</span> <span class="nf">ResponseEntity</span><span class="o">(</span><span class="n">map</span><span class="o">,</span> <span class="nc">HttpStatus</span><span class="o">.</span><span class="na">CREATED</span><span class="o">);</span>
    <span class="o">}</span>
</code></pre></div></div>

<p>위 코드는 혼자 만들어보고 있는 aiport gate관리 서버의 코드 중 일부이며, HTTP MESSAGE의 post request가 발생했을때 서버의 엔드포인트인 컨트롤러에 도달하게 되는데, 그 컨트롤러에 속해있는 post를 관리하는 핸들러 메서드이다. 이때 핸들러 메서드의 선언부를 보면 <strong><em>@Requestparam</em></strong>이라는 애너테이션이 있다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>@RequestParam
    
    @Requestparam 1.("가져올 데이터의 이름") 2.[데이터 타입] 3.[데이터를 담을 변수]
        - 이 때 1과 3이 같다면 1은 생략 가능하다.
        - 위 예시를 바탕으로하면 URI는 다음과 같을 것이다.
            컨트롤러 레벨에서의 애너테이션이 @Requestmapping("v1/gates")라면
            localhost:8080/v1/gates?number=(무엇무엇)&amp;rentstatus=(무엇무엇)&amp;price=(무엇무엇)
</code></pre></div></div>

<p>이처럼 @Requestparam 애너테이션의 경우 변수와 데이터를 1개씩 매칭시키는데, 만약 데이터의 양이 많아진다면?? 코드가 굉장히 길어질 것이다. 때문에 dto라는 requestbody를 받는 객체를 만들어서 대신 사용할 수 있다.</p>

<h3 id="dto클래스-만들기">dto클래스 만들기</h3>

<p>그러면 다음과 같이 dto클래스를 먼저 만든다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">GatePostDto</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="kt">int</span> <span class="n">number</span><span class="o">;</span>
    <span class="kd">private</span> <span class="kt">boolean</span> <span class="n">rentStatus</span><span class="o">;</span>
    <span class="kd">private</span> <span class="kt">int</span> <span class="n">price</span><span class="o">;</span>

    <span class="kd">public</span> <span class="kt">int</span> <span class="nf">getNumber</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">return</span> <span class="n">number</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">setNumber</span><span class="o">(</span><span class="kt">int</span> <span class="n">number</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">number</span> <span class="o">=</span> <span class="n">number</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="kt">boolean</span> <span class="nf">isRentStatus</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">return</span> <span class="n">rentStatus</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">setRentStatus</span><span class="o">(</span><span class="kt">boolean</span> <span class="n">rentStatus</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">rentStatus</span> <span class="o">=</span> <span class="n">rentStatus</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="kt">int</span> <span class="nf">getPrice</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">return</span> <span class="n">price</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">setPrice</span><span class="o">(</span><span class="kt">int</span> <span class="n">price</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">price</span> <span class="o">=</span> <span class="n">price</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>이때 필드변수는 requestbody가 있는. 즉, @Requestparam의 데이터를 선언하면 된다. 이를 이용하면 코드가 엄청 간단해진다.</p>

<h3 id="dto-적용-핸들러-메서드">dto 적용 핸들러 메서드</h3>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@PostMapping</span>
    <span class="kd">public</span> <span class="nc">ResponseEntity</span> <span class="nf">postGate</span><span class="o">(</span><span class="nd">@RequestBody</span> <span class="nc">GatePostDto</span> <span class="n">gatePostDto</span><span class="o">){</span>

        <span class="k">return</span> <span class="k">new</span> <span class="nf">ResponseEntity</span><span class="o">(</span><span class="n">gatePostDto</span><span class="o">,</span> <span class="nc">HttpStatus</span><span class="o">.</span><span class="na">CREATED</span><span class="o">);</span>
    <span class="o">}</span>
</code></pre></div></div>

<p>첫째로 파라미터에 앞서 만든 GatePostDto객체를 받아오기 때문에 굉장히 간단해졌다.
둘째로 첫째의 영향으로 map을 선언해서 데이터들을 담을 필요가 없어졌다.</p>

<h3 id="의문점---patch는">의문점 - patch는??</h3>

<p>여기서 의문점이 발생했다.
patch HTTP MESSAGE또한 requestbody를 가지고 있다. post가 CRUD상에서 ‘C - create’라면 patch는 일부 데이터를 변경하는 ‘U - update’이기 때문이다. 따라서 이러한 고민을 가지게 되었다.</p>

<blockquote>
  <p>patch dto를 만들면 필드면수는 어떤 것들로 채워야하는가??</p>
</blockquote>

<p>아무리 찾아봐도 없었다. 나중에 데이터베이스에 저장하게 될 하나의 레코드를 전부 다 넣어야할까?? 아니면 뭐 법칙이 있나?? 여러가지 생각이 많이 들었다. 그런 고민을(지금보면 쓸데없지만..) 하다가 결국 도달하 결론은 이것이다.</p>

<h3 id="결론">결론</h3>

<blockquote>
  <p>바꾸고 싶은 데이터 파라미터만 필드로 선언한다.</p>
</blockquote>

<p>공항의 gate의 경우를 보자. 가격과 렌트 유무를 정보로 가지고 있다. 그렇다면?? 그 두가지는 가격정책에 의해서, 항공사의 대여 유무에 의해서 바뀔 수 있는 데이터이다. 따라서 patchdto의 경우는 다음과 같이 작성하면 된다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">GatePatchDto</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="kt">long</span> <span class="n">gateId</span><span class="o">;</span>
    <span class="kd">private</span> <span class="kt">boolean</span> <span class="n">rentStatus</span><span class="o">;</span>
    <span class="kd">private</span> <span class="kt">int</span> <span class="n">price</span><span class="o">;</span>

    <span class="kd">public</span> <span class="kt">long</span> <span class="nf">getGateId</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">return</span> <span class="n">gateId</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">setGateId</span><span class="o">(</span><span class="kt">long</span> <span class="n">gateId</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">gateId</span> <span class="o">=</span> <span class="n">gateId</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="kt">boolean</span> <span class="nf">isRentStatus</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">return</span> <span class="n">rentStatus</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">setRentStatus</span><span class="o">(</span><span class="kt">boolean</span> <span class="n">rentStatus</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">rentStatus</span> <span class="o">=</span> <span class="n">rentStatus</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="kt">int</span> <span class="nf">getPrice</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">return</span> <span class="n">price</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">setPrice</span><span class="o">(</span><span class="kt">int</span> <span class="n">price</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">price</span> <span class="o">=</span> <span class="n">price</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>
<p>여기서 gateId는 컨트롤러의 patch핸들러 메서드 상에서 @Pathvariable(“~~”)의 ~로 받아 변수에 저장한 값일 것이다. 이유는 하나의 상태를 post하게 되면 게이트들을 구분하는 하나의 identification이 있어야 할 것이고, 생성 될 것이니까.</p>]]></content><author><name>Shinjae Kim</name><email>lo97729@gmail.com</email></author><category term="개인탐구" /><category term="Spring" /><summary type="html"><![CDATA[이 글에 담긴 것 patchDto의 필드변수 설정]]></summary></entry><entry><title type="html">Section 3 Spring - JDBC</title><link href="http://localhost:4000/%EC%BD%94%EB%93%9C%EC%8A%A4%ED%85%8C%EC%9D%B4%EC%B8%A0/Section3-JDBC/" rel="alternate" type="text/html" title="Section 3 Spring - JDBC" /><published>2022-08-26T00:00:00+09:00</published><updated>2022-08-26T00:00:00+09:00</updated><id>http://localhost:4000/%EC%BD%94%EB%93%9C%EC%8A%A4%ED%85%8C%EC%9D%B4%EC%B8%A0/Section3%20JDBC</id><content type="html" xml:base="http://localhost:4000/%EC%BD%94%EB%93%9C%EC%8A%A4%ED%85%8C%EC%9D%B4%EC%B8%A0/Section3-JDBC/"><![CDATA[<p>[week10]</p>
<h2 id="간단정리">간단정리</h2>
<ul>
  <li>spring data jdbc는 jdbc api를 더 손 쉽게 사용할 수 있도록 편의성을 증진시켜주는 기술이다.</li>
  <li>table과 entity mapping시에 primary key에 @id 애너테이션을 추가하자</li>
  <li>entity와 table의 이름이 다를 시 클래스 레벨에 @table(“이름”) 애너테이션을 추가하자</li>
  <li>관계설정을 위해서 scheme 파일에 테이블을 설계해주어야 한다.</li>
</ul>

<p><br /></p>

<h2 id="느낀점">느낀점</h2>
<p>단순히 지식을 구현하는 부분이 아니라는 것을 뼈저리게 느낀다. controller와 service의 경우보다 내가 직접 설계해보고 구현해보아야 더욱 더 내 것이 되는 영역인 것 같다.</p>

<p><br /></p>

<h2 id="스스로-답해보기">스스로 답해보기</h2>
<ul>
  <li>JDBC는 무엇인가</li>
  <li>ORM은 무엇인가</li>
  <li>spring data JDBC는 무엇인가?</li>
</ul>

<p><br /></p>

<h2 id="정리---들어가며">정리 - 들어가며</h2>

<p>JDBC - Java Database Connectivity</p>

<p>이름을 직역해보자. - 자바와 데이터베이스의 연결 -</p>

<p>맞다. 우리가 자바언어의 코드레벨에서 데이터베이스를 사용 할 수 있게 해주는 것이다.</p>

<p>정확히 말하면 위 목적을 위한 표준 사양이다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>표준사양

pojo를 생각해보면 된다. 해당 사양을 구현하는 여러 벤더사들이 있을 것인데,
표준사양이 아닌 벤더사의 기술을 그대로 코드에 적용한다면
1. pojo가 아니게 될 것이며
2. 그래서 유지 보수, 개선이 힘들 것이다.
하지만 표준 사양을 구현한 벤더사의 기술들을 사용하면
DI를 통해 framework의 자유로운 사용이 가능하다

- 그냥 나만의 언어로 표현한 것
</code></pre></div></div>

<blockquote>
  <p>이번에 학습하게 될 Srping data JDBC는 이런 jdbc api를 쉽게 사용하게 해주는 기술이다.</p>
</blockquote>

<p>그러니,</p>

<p>JDBC api를 구체적으로 파고들어갈 당위성은 당장은 없지만, 어떤 흐름인지는 알면 좋겠다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1) JDBC API
2) JDBC 드라이버
    데이터베이스와의 통신을 담당하는 인터페이스
    해당 벤더사에 맞는 jdbc 드라이버가 구현되어 제공될 것
3) 데이터베이스
</code></pre></div></div>

<p>2)의 JDBC드라이버는 <strong><em>데이터베이스와의 통신을 담당한다고</em></strong> 했다.</p>

<p>그 통신 과정을 구체화해보면</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1) connection객체 생성  -  데이터베이스와의 연결 세션
2) statement객체 생성  -  그 안에서 sql query문 작성
3) resultset객체로부터 데이터 조회  -  query에 따른 결과
4) 역순으로 close
</code></pre></div></div>

<p>이때 connection객체 생성은 비용이 많이 드는 작업이기 때문에, 애플리케이션 로딩 시점에 미리 생성하여 연결이 필요할때마다 제공한다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>연결이 필요하네?? connection객체 만들어 -&gt; x
연결이 필요하네?? connection객체 있나요?? -&gt; o
</code></pre></div></div>

<p>이렇게 connection을 제공하고 반환받는 일련의 과정을 담당하는 것을 connection pool이라고 한다.</p>

<hr />

<h3 id="1-spring-data-jdbc">1. Spring Data JDBC</h3>

<blockquote>
  <p>ORM - Object-Relational-Mapping</p>
</blockquote>

<p>Spring Data JDBC는 ORM기술이며, ORM기술은 query를 코드상에서 직접적으로 작성하는 기술이 아닌, <strong><em>객체를 통해 데이터베이스에 접근</em></strong> 하는 기술이다.</p>

<p>객체는 내부적으로 SQL 쿼리문으로 자동 변환된다.</p>

<p>그리고</p>

<p>이 기술을 사용하기 위해서는 다음의 의존 라이브러리를 추가한다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>implementation 'org.springframework.boot:spring-boot-starter-data-jdbc'
</code></pre></div></div>

<hr />

<h3 id="2-데이터-액세스-계층-구현">2. 데이터 액세스 계층 구현</h3>

<p>우리가 동물의 정보를 담는 entity를 만들었다고 해보자. (컨트롤러와 Dto는 있다고 가정)</p>

<p>-Entity</p>
<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code>
<span class="nd">@Data</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">Animal</span><span class="o">{</span>

    <span class="nd">@Id</span> <span class="c1">// 식별자로 사용 -&gt; 기본키</span>
    <span class="kd">private</span> <span class="nc">Long</span> <span class="n">animalId</span><span class="o">;</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">species</span><span class="o">;</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">name</span><span class="o">;</span>
<span class="o">}</span>
</code></pre></div></div>

<p>-Service</p>
<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code>
<span class="nd">@Service</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">AnimalService</span><span class="o">{</span>

    <span class="kd">public</span> <span class="nc">Animal</span> <span class="nf">creatAnimal</span><span class="o">(</span><span class="nc">Animal</span> <span class="n">animal</span><span class="o">){</span>

        <span class="k">return</span> <span class="n">animal</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>그러면 Repository를 만들어보자!</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="mi">1</span><span class="o">)</span>
<span class="kd">public</span> <span class="kd">interface</span> <span class="nc">AnimalRepository</span> <span class="kd">extends</span> <span class="nc">CrudRepository</span><span class="o">&lt;</span><span class="nc">Animal</span><span class="o">,</span> <span class="nc">Long</span><span class="o">&gt;{</span>

    <span class="mi">2</span><span class="o">)</span>
    <span class="nc">Optional</span><span class="o">&lt;</span><span class="nc">Animal</span><span class="o">&gt;</span> <span class="nf">findByName</span><span class="o">(</span><span class="nc">String</span> <span class="n">name</span><span class="o">);</span>
<span class="o">}</span>

</code></pre></div></div>

<p>일단 1번에서 볼 수 있듯 repository는 class가 아닌 repository이다.</p>

<p>또한</p>

<p>CrudRepository를 상속함으로써, CrueRepository에의 save, findById등의 기본 메서드들을 사용할 수 있게 된다.</p>

<p>그리고</p>

<p>2번을 보면 찾은 Animal을 Optional로 감쌌다. 이렇게 함으로써 null(찾지 못함)이 발생했을 경우에도 nullpointerException없이 진행이 가능하다.</p>

<p>또한</p>

<p>메서드는 query처럼 작성해도 알아서 알아듣게 되는데, findBy~~ -&gt;  By 다음에 기준이 되는 field의 이름을 적어주도록 하자.</p>

<hr />

<h3 id="3-연동">3. 연동</h3>

<p>이제 Service 계층으로 가서 연동을 해보자.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code>
<span class="nd">@Service</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">AnimalService</span><span class="o">{</span>

    <span class="mi">1</span><span class="o">)</span>
    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">AnimalRepository</span> <span class="n">animalRepository</span><span class="o">;</span>

    <span class="kd">public</span> <span class="nf">AnimalService</span><span class="o">(</span><span class="nc">AnimalRepository</span> <span class="n">animalRepository</span><span class="o">){</span>
        <span class="k">this</span><span class="o">.</span><span class="na">animalRepository</span> <span class="o">=</span> <span class="n">animalRepository</span>
    <span class="o">}</span>

    <span class="mi">2</span><span class="o">)</span>
    <span class="kd">public</span> <span class="nc">Animal</span> <span class="nf">creatAnimal</span><span class="o">(</span><span class="nc">Animal</span> <span class="n">animal</span><span class="o">){</span>
        
        <span class="n">verifyExist</span><span class="o">(</span><span class="n">animal</span><span class="o">.</span><span class="na">getName</span><span class="o">());</span>

        <span class="k">return</span> <span class="n">animalRepository</span><span class="o">.</span><span class="na">save</span><span class="o">(</span><span class="n">animal</span><span class="o">);</span>
    <span class="o">}</span>

    <span class="mi">3</span><span class="o">)</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">verifyExist</span><span class="o">(</span><span class="nc">String</span> <span class="n">name</span><span class="o">){</span>
        <span class="nc">Optional</span><span class="o">&lt;</span><span class="nc">Animal</span><span class="o">&gt;</span> <span class="n">animal</span> <span class="o">=</span> <span class="n">animalRepository</span><span class="o">.</span><span class="na">findByName</span><span class="o">(</span><span class="n">name</span><span class="o">);</span>

        <span class="k">if</span><span class="o">(</span><span class="n">animal</span><span class="o">.</span><span class="na">isPresent</span><span class="o">())</span>
            <span class="k">throw</span> <span class="k">new</span> <span class="nf">BusinessLogicException</span><span class="o">(</span><span class="nc">ExceptionCode</span><span class="o">.</span><span class="na">ANIMAL_EXISTS</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>1번을 보면 Controller에서 Service를 DI받았듯, respository의 기능을 이용하기 위해 animalRepository를 DI 받는다.</p>

<p>2번을 보면 갑자기 뜬금없이 verifyExist() 메서드가 보인다. 이것은 3번에서 설명하도록 하겠다.
그리고
반환값으로 animalRepository.save(animal) -&gt; crudrepository를 상속받은 animalrepository의 메서드를 호출한다.</p>

<p>3번은 service계층에서 이용하기 위해 따로 작성한 메서드로, 논리적인 생각이 뒤따라야 이해하기 편하다
일단 가정을 곰곰히 생각해보고 가자 -&gt; ‘동물의 이름은 중복 될 수 없다’
웃기긴 하지만 저렇게 가정을 했다고 해보자
그리고 클라이언트로부터 <strong><em>이미 있는 이름</em></strong>의 동물을 등록하려고 한다면 가정과 모순되는 상황이 발생한다.
따라서 repository에서 이름으로 동물을 찾아와서
이미 데이터베이스에 존재하는지 확인하는 메서드이다.</p>

<p>또한</p>

<p>Optional로 반환받은 값을 그대로 Optional에 담아 받았기 때문에 해당 데이터가 null인지 아닌지 여부에 따른 코드 구성을 손쉽게 할 수 있다.</p>

<hr />

<h3 id="3-테이블-간의-관계-매핑">3. 테이블 간의 관계 매핑</h3>

<p>도메인과 애그리거트라는 개념이 있다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>도메인 - 현실이 한 업무 영역
애그리커트 - 관련 도메인들의 집합
    애그리거트 루트 - 대표 도메인
</code></pre></div></div>

<p>그리고 이 두가지 개념이 연관관계 매핑의 기준이 된다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1. 모든 접근은 애그리거트 루트를 통해서 이루어진다.

2. 같은 애그리거트 안에서는 엔티티간 객체로 참조한다

3. 애그래거트 루트 대 애그리거트 루트 간에는
    애그리거트 루트 간의 참조는 객체 참조 대신에 ID로 참조한다.
    1대1과 1대N 관계일 때는 테이블 간의 외래키 방식과 동일하다.
    N대N 관계일 때는 외래키 방식인 ID 참조와 객체 참조 방식이 함께 사용된다.    
</code></pre></div></div>

<p>방법론을 한번에 정리해보면</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code>
<span class="c1">// 1. 1:N - 애그리거트 루트 간</span>
    <span class="c1">// 1 . 기본키가 될 멤버에</span>
    <span class="nd">@Id</span>
    <span class="kd">private</span> <span class="nc">Long</span> <span class="n">ooId</span><span class="o">;</span>

    <span class="c1">// N . AggregateReference가 외래키 역할</span>
    <span class="kd">private</span> <span class="nc">AggregateReference</span><span class="o">&lt;</span><span class="n">테이블이름</span><span class="o">,</span> <span class="nd">@Id가</span> <span class="n">붙은</span> <span class="n">멤버의</span> <span class="n">타입</span><span class="o">&gt;</span> <span class="n">ooId</span><span class="o">;</span>
    <span class="c1">// </span>

<span class="c1">// 2. N:N</span>
    <span class="c1">// 1:N , N:1로 만들어 준다.</span>
    <span class="c1">// 동일 애그리커트 안의 1:N</span>
        <span class="c1">//1. MappedCollection으로 객체참조</span>
        <span class="nd">@MappedCollection</span><span class="o">(</span><span class="n">idColumn</span> <span class="o">=</span> <span class="s">"OO_ID"</span><span class="o">)</span> <span class="c1">// &lt;- 테이블에 해당하는 column의 이름</span>
        <span class="kd">private</span> <span class="n">set</span><span class="o">&lt;</span><span class="nc">N해당</span> <span class="n">클래스</span><span class="o">&gt;</span> <span class="n">ooo</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">LinkedHashSet</span><span class="o">&lt;&gt;();</span>
        
        <span class="c1">//N. 그냥 다른 애그리커트의 1에 해당하는 엔티티의 id를 추가</span>
        <span class="n">pirvate</span> <span class="nc">Long</span> <span class="n">coffeeId</span><span class="o">;</span>

</code></pre></div></div>

<p>정리가 참 힘들다. 그래도 다시 해보면</p>

<p><strong>같은 애그리거트면</strong> 객체참조(1에 list나 set으로 객체참조)하고</p>

<p><strong>다른 애그리거트 루트끼리</strong> 1:1 혹은 1:N이면 외래키 참조(AggregateReference) 한다.</p>]]></content><author><name>Shinjae Kim</name><email>lo97729@gmail.com</email></author><category term="코드스테이츠" /><category term="Spring" /><summary type="html"><![CDATA[[week10] 간단정리 spring data jdbc는 jdbc api를 더 손 쉽게 사용할 수 있도록 편의성을 증진시켜주는 기술이다. table과 entity mapping시에 primary key에 @id 애너테이션을 추가하자 entity와 table의 이름이 다를 시 클래스 레벨에 @table(“이름”) 애너테이션을 추가하자 관계설정을 위해서 scheme 파일에 테이블을 설계해주어야 한다.]]></summary></entry><entry><title type="html">Section 3 Spring - 예외처리(비즈니스로직)</title><link href="http://localhost:4000/%EC%BD%94%EB%93%9C%EC%8A%A4%ED%85%8C%EC%9D%B4%EC%B8%A0/Section3-%EC%98%88%EC%99%B8%EC%B2%98%EB%A6%AC(%EC%84%9C%EB%B9%84%EC%8A%A4)/" rel="alternate" type="text/html" title="Section 3 Spring - 예외처리(비즈니스로직)" /><published>2022-08-25T00:00:00+09:00</published><updated>2022-08-25T00:00:00+09:00</updated><id>http://localhost:4000/%EC%BD%94%EB%93%9C%EC%8A%A4%ED%85%8C%EC%9D%B4%EC%B8%A0/Section3%20%EC%98%88%EC%99%B8%EC%B2%98%EB%A6%AC(%EC%84%9C%EB%B9%84%EC%8A%A4)</id><content type="html" xml:base="http://localhost:4000/%EC%BD%94%EB%93%9C%EC%8A%A4%ED%85%8C%EC%9D%B4%EC%B8%A0/Section3-%EC%98%88%EC%99%B8%EC%B2%98%EB%A6%AC(%EC%84%9C%EB%B9%84%EC%8A%A4)/"><![CDATA[<p>[week10]</p>
<h2 id="간단정리">간단정리</h2>
<ul>
  <li>사용하고자 하는 exception을 enum으로 정의하자</li>
  <li>그리고 businessException과 같은 RuntimeException을 상속하는 클래스를 만들자</li>
  <li>ErrorResponse를 통해 가공하고</li>
  <li>@RestControllerAdvice에게 넘겨주자</li>
</ul>

<p><br /></p>

<h2 id="느낀점">느낀점</h2>
<p>예외처리를 이해하는데 시간이 꽤나 걸렸다. 그래도 이해하니까, solid원칙과, 흐름이 조금이나마 내 지식에 저장된 느낌이 든다.</p>

<p><br /></p>

<h2 id="스스로-답해보기">스스로 답해보기</h2>
<ul>
  <li>api계층에서 발생하는 예외</li>
  <li>비즈니스 계층에서 발생하는 예외</li>
</ul>

<p><br /></p>

<h2 id="정리---들어가며">정리 - 들어가며</h2>
<p>이번 정리는 두가지 경우의 예외와 관련이 있다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1. DTO에 적용한 유효성 검증 실패
2. 비즈니스 로직에서 의도적으로 던저지는 예외
</code></pre></div></div>

<p>간단하게 생각한다면 두 경우 모두 어떤 예외상황이 발생할 경우 알게 되며,</p>

<p><strong><em>예외 발생시 메시지를 던진다.</em></strong></p>

<p>다른 점은 1번은 API계층에서 발생하고, 2번은 서비스 계층에서 발생하여 의도적으로 API계층에서 관리된다.</p>

<p>우리가 할 것은 이 메시지를 알기 쉽게 전달하는 것이 되겠다.</p>

<blockquote>
  <p>이번 글은 2번을 중점으로 다룬다.</p>
</blockquote>

<h3 id="1-서비스-계층에서-예외가-발생하면">1. 서비스 계층에서 예외가 발생하면??</h3>

<p>서비스 계층에서 비즈니스 로직을 작성하다가 이런 생각을 했다면 어떨까??</p>

<p>‘흠.. 여기서는 이러한 에러가 예상이 되는데??’</p>

<p>그러면 <strong><em>throws</em></strong> 를 이용할 수 있을 것이다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>throws 예외를 호출한 지점으로 던진다.
</code></pre></div></div>

<p>즉, 앞선 글에서 예외를 담당했던 클래스인 @RestControllerAdvice 애너테이션이 붙은 클래스에서 예외를 담당하게 된다.</p>

<h3 id="2-enum-만들기">2 ENUM 만들기</h3>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">enum</span> <span class="nc">ExceptionCode</span> <span class="o">{</span>
    <span class="no">MEMBER_NOT_FOUND</span><span class="o">(</span><span class="mi">404</span><span class="o">,</span> <span class="s">"Member Not Found"</span><span class="o">);</span>

    <span class="nd">@Getter</span>
    <span class="kd">private</span> <span class="kt">int</span> <span class="n">status</span><span class="o">;</span>

    <span class="nd">@Getter</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">message</span><span class="o">;</span>

    <span class="nc">ExceptionCode</span><span class="o">(</span><span class="kt">int</span> <span class="n">status</span><span class="o">,</span> <span class="nc">String</span> <span class="n">message</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">status</span> <span class="o">=</span> <span class="n">status</span><span class="o">;</span>
        <span class="k">this</span><span class="o">.</span><span class="na">message</span> <span class="o">=</span> <span class="n">message</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>member를 찾지 못했을 경우 발생할 exception을 enum으로 정의한다.</p>

<p>그렇다면</p>

<p>왜 enum으로 정의하는가??</p>

<p>이 enum을 사용해서 예외를 정의해두면, 추후 예외를 관리할때 매우 편리해진다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">BusinessLogicException</span> <span class="kd">extends</span> <span class="nc">RuntimeException</span> <span class="o">{</span>
    <span class="nd">@Getter</span>
    <span class="kd">private</span> <span class="nc">ExceptionCode</span> <span class="n">exceptionCode</span><span class="o">;</span>

    <span class="kd">public</span> <span class="nf">BusinessLogicException</span><span class="o">(</span><span class="nc">ExceptionCode</span> <span class="n">exceptionCode</span><span class="o">)</span> <span class="o">{</span>
        <span class="kd">super</span><span class="o">(</span><span class="n">exceptionCode</span><span class="o">.</span><span class="na">getMessage</span><span class="o">());</span>
        <span class="k">this</span><span class="o">.</span><span class="na">exceptionCode</span> <span class="o">=</span> <span class="n">exceptionCode</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>
<p>RuntimeException을 상속하는 BusinessLogicException이다.</p>

<p>부모클래스로 자식클래스를 인스턴스화 할 수 있었다.</p>

<p>그러니 비즈니스 로직 실행 도중 RuntimeException이 발생하면 BusinessLogicException으로 구체화할 수 있다.</p>

<p>그리고</p>

<p>이 BusinessLogicException을 생성할때 위에서 정의한 ExceptionCode를 전달하면
멤버로 super에 포함된 message와 ExceptionCode를 가지게 된다.</p>

<p>이를</p>

<p><strong><em>에러 메시지를 가공하던 ErrorRespons로 넘겨보자</em></strong></p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">ErrorResponse</span> <span class="o">{</span>

    <span class="kd">private</span> <span class="kt">int</span> <span class="n">status</span><span class="o">;</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">message</span><span class="o">;</span>
    <span class="kd">private</span> <span class="nc">List</span><span class="o">&lt;</span><span class="nc">FieldError</span><span class="o">&gt;</span> <span class="n">fieldErrors</span><span class="o">;</span>
    <span class="kd">private</span> <span class="nc">List</span><span class="o">&lt;</span><span class="nc">ConstraintViolationError</span><span class="o">&gt;</span> <span class="n">violationErrors</span><span class="o">;</span>

    <span class="o">.</span>
    <span class="o">.</span>
    <span class="o">.</span>

    <span class="kd">private</span> <span class="nf">ErrorResponse</span><span class="o">(</span><span class="kt">int</span> <span class="n">status</span><span class="o">,</span> <span class="nc">String</span> <span class="n">message</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">status</span> <span class="o">=</span> <span class="n">status</span><span class="o">;</span>
        <span class="k">this</span><span class="o">.</span><span class="na">message</span> <span class="o">=</span> <span class="n">message</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="o">.</span>
    <span class="o">.</span>
    <span class="o">.</span>

    <span class="kd">public</span> <span class="kd">static</span> <span class="nc">ErrorResponse</span> <span class="nf">of</span><span class="o">(</span><span class="nc">ExceptionCode</span> <span class="n">exceptionCode</span><span class="o">){</span>
        <span class="k">return</span> <span class="k">new</span> <span class="nf">ErrorResponse</span><span class="o">(</span><span class="n">exceptionCode</span><span class="o">.</span><span class="na">getStatus</span><span class="o">(),</span> <span class="n">exceptionCode</span><span class="o">.</span><span class="na">getMessage</span><span class="o">());</span>
    <span class="o">}</span>
</code></pre></div></div>

<p>이렇게 추가해주면 ErrorResponse에 status와 message를 담을 수 있다.</p>

<p>생성자는 private으로 지정해두었기 때문에 외부에서 인스턴스화가 불가능하며</p>

<p>따라서</p>

<p>of 메서드에 전달 받은 exceptionCode를 받았을 경우 내부에서 생성자를 호출하여</p>

<p>상태와 메시지를 담는 식으로 가공하게 된다.</p>

<p>그러면 이것을</p>

<p>@RestControllerAdvice로 전달하면</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@RestControllerAdvice</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">GlobalExceptionAdvice</span> <span class="o">{</span>

    
    <span class="nd">@ExceptionHandler</span>
    <span class="kd">public</span> <span class="nc">ResponseEntity</span><span class="o">&lt;&gt;</span> <span class="nf">handleMethodArgumentNotValidException</span><span class="o">(</span>
            <span class="nc">BusinessException</span> <span class="n">e</span><span class="o">)</span> <span class="o">{</span>
        <span class="kd">final</span> <span class="nc">ErrorResponse</span> <span class="n">response</span> <span class="o">=</span> <span class="nc">ErrorResponse</span><span class="o">.</span><span class="na">of</span><span class="o">(</span><span class="n">e</span><span class="o">.</span><span class="na">exceptionCode</span><span class="o">);</span>

        <span class="k">return</span> <span class="k">new</span> <span class="nf">ResponseEntity</span><span class="o">(</span><span class="n">response</span><span class="o">,</span> <span class="nc">HttpStatus</span><span class="o">.</span><span class="na">valueOf</span><span class="o">(</span><span class="n">e</span><span class="o">.</span><span class="na">getExceptionCode</span><span class="o">().</span><span class="na">getStatus</span><span class="o">()));</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>RuntimeException을 상속받은 우리가 구체화한 BusinessException을 던져주면 된다.</p>

<p>BusinessException의 생성자를 생각해보면 exceptionCode를 받았기 때문에</p>

<p>예외를 던져줄때 enum에 사용 하고자하는 exception을 정의한 후 던져주면 되겠다.</p>

<p>또한</p>

<p>exceptioncode가 많아지면 그에 따른 status도 다양해진다. 이 상태를 동적으로 전달하기위해 @ResponseStatus로 고정하지 않고 ResponseEntity에 HttpStatus.valueOf()에 담아준다.</p>]]></content><author><name>Shinjae Kim</name><email>lo97729@gmail.com</email></author><category term="코드스테이츠" /><category term="Spring" /><summary type="html"><![CDATA[[week10] 간단정리 사용하고자 하는 exception을 enum으로 정의하자 그리고 businessException과 같은 RuntimeException을 상속하는 클래스를 만들자 ErrorResponse를 통해 가공하고 @RestControllerAdvice에게 넘겨주자]]></summary></entry><entry><title type="html">Section 3 Spring - 예외처리</title><link href="http://localhost:4000/%EC%BD%94%EB%93%9C%EC%8A%A4%ED%85%8C%EC%9D%B4%EC%B8%A0/Section-3-%EC%98%88%EC%99%B8%EC%B2%98%EB%A6%AC/" rel="alternate" type="text/html" title="Section 3 Spring - 예외처리" /><published>2022-08-24T00:00:00+09:00</published><updated>2022-08-24T00:00:00+09:00</updated><id>http://localhost:4000/%EC%BD%94%EB%93%9C%EC%8A%A4%ED%85%8C%EC%9D%B4%EC%B8%A0/Section%203%20%EC%98%88%EC%99%B8%EC%B2%98%EB%A6%AC</id><content type="html" xml:base="http://localhost:4000/%EC%BD%94%EB%93%9C%EC%8A%A4%ED%85%8C%EC%9D%B4%EC%B8%A0/Section-3-%EC%98%88%EC%99%B8%EC%B2%98%EB%A6%AC/"><![CDATA[<p>[week10]</p>
<h2 id="간단정리">간단정리</h2>
<ul>
  <li>@ExceptionHandler를 이용하여 예외를 처리하자.</li>
  <li>@RestControllerAdvice를 이용하여 예외처리를 통합하자.(중복을 제거하자)</li>
  <li>예외의 가공을 담당하는 클래스를 만들자 (여기서는 ErrorResponse)</li>
</ul>

<p><br /></p>

<h2 id="느낀점">느낀점</h2>
<p>예외처리를 이해하는데 시간이 꽤나 걸렸다. 그래도 이해하니까, solid원칙과, 흐름이 조금이나마 내 지식에 저장된 느낌이 든다.</p>

<p><br /></p>

<h2 id="스스로-답해보기">스스로 답해보기</h2>
<ul>
  <li>api계층에서 발생하는 예외</li>
  <li>비즈니스 계층에서 발생하는 예외</li>
</ul>

<p><br /></p>

<h2 id="정리---들어가며">정리 - 들어가며</h2>
<p>이번 정리는 두가지 경우의 예외와 관련이 있다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1. DTO에 적용한 유효성 검증 실패
2. 비즈니스 로직에서 의도적으로 던저지는 예외
</code></pre></div></div>

<p>간단하게 생각한다면 두 경우 모두 어떤 예외상황이 발생할 경우 알게 되며,</p>

<p><strong><em>예외 발생시 메시지를 던진다.</em></strong></p>

<p>다른 점은 1번은 API계층에서 발생하고, 2번은 서비스 계층에서 발생하여 의도적으로 API계층에서 관리된다.</p>

<p>우리가 할 것은 이 메시지를 알기 쉽게 전달하는 것이 되겠다.</p>

<blockquote>
  <p>이번 글은 1번을 중점으로 다룬다.</p>
</blockquote>

<h3 id="1-api계층-예외-전달하기">1. api계층 예외 전달하기</h3>

<p>앞서 DTO를 클래스를 작성한 것을 떠올려보자. DTO클래스는 클라이언트의 요청,응답시 데이터 전달을 위한 클래스이며, 특히 요청상황에 ‘유효성 검사’의 역할을 담당한다.</p>

<p>그렇다면 유효성 검사에 실패하면 어떤 일이 벌어질까??</p>

<blockquote>
  <p>spring이 에러 메시지를 전달한다.</p>
</blockquote>

<p>하지만 우리는 이 메시지를 커스텀하여 필요한 정보만 전달 할 수 있다. 즉, <strong><em>가공</em></strong>할 수 있다.</p>

<ul>
  <li>
    <p>발생 가능 에러</p>

    <ul>
      <li>
        <p>MethodArgumentNotValidException 
  유효하지 않은 데이터를 전달 받았을때</p>
      </li>
      <li>
        <p>ConstraintViolationException
  pathvariable에 유효하지 않은 데이터를 전달 받았을 때</p>
      </li>
    </ul>
  </li>
</ul>

<h4 id="1-1-exceptionhandler">1-1. @ExceptionHandler</h4>

<blockquote>
  <p>에러메시지를 응답메시지로 돌려주는 법</p>
</blockquote>

<p>위 경우 Controller의 HandlerMethod에 데이터가 전달 되는 과정에서 발생하는 오류이기에,
Controller에서 예외를 받아 처리하는 메서드를 구현하면 된다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@ExceptionHandler</span>
<span class="kd">public</span> <span class="nc">ResponseEntity</span> <span class="nf">handleException</span><span class="o">(</span><span class="nc">MethondArgumentNotValidException</span> <span class="n">e</span><span class="o">){</span>
    <span class="mi">1</span><span class="o">)</span> <span class="kd">final</span> <span class="nc">List</span><span class="o">&lt;</span><span class="nc">FieldError</span><span class="o">&gt;</span> <span class="n">fieldErrors</span> <span class="o">=</span> <span class="n">e</span><span class="o">.</span><span class="na">getBindingResult</span><span class="o">().</span><span class="na">getFieldErrors</span><span class="o">;</span>

    <span class="mi">2</span><span class="o">)</span> <span class="k">return</span> <span class="k">new</span> <span class="nc">ResponseEntity</span><span class="o">&lt;&gt;(</span><span class="n">fieldErrors</span><span class="o">,</span> <span class="nc">HttpStatus</span><span class="o">.</span><span class="na">BAD_REQUEST</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div>

<p>1번의 List의 데이터 타입이 된 FieldError는 스프링 프레임워크의 validation 의존성으로부터 가져온 것이다.
    org.springframework.validation.FieldError
그리고
List로 담는 이유는 에러는 여러개가 한번에 발생할 수 있기 때문이다.
    DTO의 field 여러 곳에서 동시 다발적으로 유효성 검증에 실패하면 에러메시지는 여러개가 발생한다.</p>

<p>2번을 보면 ResponseEntity에 인스턴스를 담는다. 그 이유는 핸들러메서드에서와 같이 상태메시지를 함께 전달하기 위함이다. 그리고 @ResponseBody 애너테이션을 추가 할 이유도 없다.</p>

<h4 id="1-2-errorresponse">1-2. ErrorResponse</h4>

<blockquote>
  <p>에러메시지를 골라담아서 만들자.</p>
</blockquote>

<p>1-1의 ExceptionHandler를 이용하면 뭔가 가시적인 응답으로 에러 메시지를 전달할 수 있지만, 불필요한 필드들을 다 주기 때문에 아직 완전하다고 말할 수 는 없을 것이다.</p>

<p>ExceptionHandler로 돌려준 응답 메시지를 보면
    codes, arguments, defaultMessage, objectName, field, rejectedValue, bindingFilure</p>

<p>이렇게 많은 내용들이 담겨 있는데</p>

<p>여기서 가독성 향상을 위해 field, rejcetedValue , defaultMessage만 담아보자.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Getter</span>
<span class="nd">@AllArgConstructor</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">ErrorResponse</span>
    <span class="mi">1</span><span class="o">)</span>
    <span class="kd">private</span> <span class="nc">List</span><span class="o">&lt;</span><span class="nc">FieldError</span><span class="o">&gt;</span> <span class="n">fieldErrors</span><span class="o">;</span>

    <span class="mi">2</span><span class="o">)</span>
    <span class="nd">@Getter</span>
    <span class="nd">@AllArgsConstructor</span>
    <span class="kd">public</span> <span class="kd">static</span> <span class="kd">class</span> <span class="nc">FieldError</span> <span class="o">{</span>
        <span class="kd">private</span> <span class="nc">String</span> <span class="n">field</span><span class="o">;</span>
        <span class="kd">private</span> <span class="nc">Object</span> <span class="n">rejectedValue</span><span class="o">;</span>
        <span class="kd">private</span> <span class="nc">String</span> <span class="n">reason</span><span class="o">;</span>
    <span class="o">}</span>
</code></pre></div></div>

<p>1번의 FieldError는 validation의존성에서 가져온 것이 아니라, ErrorResponse안에 만든 정적 클래스 2번 FieldError를 뜻한다.</p>

<p>그러면 이를 어떻게 이용해볼까</p>

<h4 id="1-3-exceptionhandler--errorresponse">1-3 @ExceptionHandler + ErrorResponse</h4>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@ExceptionHandler</span>
<span class="kd">public</span> <span class="nc">ResponseEntity</span> <span class="nf">handleException</span><span class="o">(</span><span class="nc">MethondArgumentNotValidException</span> <span class="n">e</span><span class="o">){</span>
    <span class="mi">1</span><span class="o">)</span>
    <span class="kd">final</span> <span class="nc">List</span><span class="o">&lt;</span><span class="nc">FieldError</span><span class="o">&gt;</span> <span class="n">fieldErrors</span> <span class="o">=</span> <span class="n">e</span><span class="o">.</span><span class="na">getBindingResult</span><span class="o">().</span><span class="na">getFieldErrors</span><span class="o">;</span>

    <span class="mi">2</span><span class="o">)</span>
    <span class="nc">List</span><span class="o">&lt;</span><span class="nc">ErrorResponse</span><span class="o">.</span><span class="na">FieldError</span><span class="o">&gt;</span> <span class="n">errors</span> <span class="o">=</span>
            <span class="n">fieldErrors</span><span class="o">.</span><span class="na">stream</span><span class="o">()</span>
                        <span class="o">.</span><span class="na">map</span><span class="o">(</span><span class="n">error</span> <span class="o">-&gt;</span> <span class="k">new</span> <span class="nc">ErrorResponse</span><span class="o">.</span><span class="na">FieldError</span><span class="o">(</span>
                            <span class="n">error</span><span class="o">.</span><span class="na">getField</span><span class="o">(),</span>
                            <span class="n">error</span><span class="o">.</span><span class="na">getRejectedValue</span><span class="o">(),</span>
                            <span class="n">error</span><span class="o">.</span><span class="na">getDefaultMessage</span><span class="o">()</span>
                        <span class="o">))</span>
                        <span class="o">.</span><span class="na">collect</span><span class="o">(</span><span class="nc">Collectors</span><span class="o">.</span><span class="na">toList</span><span class="o">());</span>

                                    <span class="mi">3</span><span class="o">)</span>
    <span class="k">return</span> <span class="k">new</span> <span class="nc">ResponseEntity</span><span class="o">&lt;&gt;(</span><span class="k">new</span> <span class="nc">ErrorResponse</span><span class="o">(</span><span class="n">errors</span><span class="o">),</span> <span class="nc">HttpStatus</span><span class="o">.</span><span class="na">BAD_REQUEST</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div>
<p>우선 1번과 2번의 FieldError는 다르다. 그 이유는 위에서 설명했고,</p>

<p>흐름으로 보면</p>

<p>1번에서 예외 발생 시 메시지를 날 것 그대로 가져온다.
그리고 
2번의 ErrorResponse.FieldError에 담는다. (FieldError는 static 클래스로 ErrorResponse를 인스턴스화 하지 않아도 호출 가능하다.)
그리고
3번과 같이 ErrorResponse의 생성자로 errors를 넣어주면서 ResponseEntity에 담는다.</p>

<p><strong>문제 발생</strong>
생각해보자.</p>

<p>우선, handlerException메서드를 컨트롤러 클래스마다 중복 작성해야 한다.</p>

<p>또한, handlerExcpetion메서드가 두가지 역할을 담당하고있다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1. 예외메시지 전달
2. 예외메시지 가공
</code></pre></div></div>

<hr />

<h3 id="2-restcontrolleradvice">2. @RestControllerAdvice</h3>

<blockquote>
  <p>문제 : 중복작성해야 하며, 하나의 클래스가 두가지 역할을 담당하고 있음</p>
</blockquote>

<p>문제들을 해결해보자. 일단은 모든 클래스들의 예외상황을 받아서 예외 메시지를 전달만 할 클래스이다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@RestControllerAdvice</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">GlobalExceptionAdvice</span> <span class="o">{</span>

    <span class="mi">1</span><span class="o">)</span>
    <span class="nd">@ExceptionHandler</span>
    <span class="nd">@ResponseStatus</span><span class="o">(</span><span class="nc">HttpStatus</span><span class="o">.</span><span class="na">BAD_REQUEST</span><span class="o">)</span>
    <span class="kd">public</span> <span class="nc">ErrorResponse</span> <span class="nf">handleMethodArgumentNotValidException</span><span class="o">(</span>
            <span class="nc">MethodArgumentNotValidException</span> <span class="n">e</span><span class="o">)</span> <span class="o">{</span>
        <span class="kd">final</span> <span class="nc">ErrorResponse</span> <span class="n">response</span> <span class="o">=</span> <span class="nc">ErrorResponse</span><span class="o">.</span><span class="na">of</span><span class="o">(</span><span class="n">e</span><span class="o">.</span><span class="na">getBindingResult</span><span class="o">());</span>

        <span class="k">return</span> <span class="n">response</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="mi">2</span><span class="o">)</span>
    <span class="nd">@ExceptionHandler</span>
    <span class="nd">@ResponseStatus</span><span class="o">(</span><span class="nc">HttpStatus</span><span class="o">.</span><span class="na">BAD_REQUEST</span><span class="o">)</span>
    <span class="kd">public</span> <span class="nc">ErrorResponse</span> <span class="nf">handleConstraintViolationException</span><span class="o">(</span>
            <span class="nc">ConstraintViolationException</span> <span class="n">e</span><span class="o">)</span> <span class="o">{</span>
        <span class="kd">final</span> <span class="nc">ErrorResponse</span> <span class="n">response</span> <span class="o">=</span> <span class="nc">ErrorResponse</span><span class="o">.</span><span class="na">of</span><span class="o">(</span><span class="n">e</span><span class="o">.</span><span class="na">getConstraintViolations</span><span class="o">());</span>

        <span class="k">return</span> <span class="n">response</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>1번의 경우는 유효성 검사 실패시, 2번의 경우는 pathvariable으로 잘못된 값이 전달 되었을 시에 처리를 담당할 메서드가 된다.</p>

<p>그리고 각각의 에러 메시지는 정해진 status가 있기에 @ResponseStatus를 이용한다.</p>

<p>그렇다면 에러메시지를 가공하는 것은 어디서 할까??</p>

<p>그건 바로</p>

<p>ErrorResponse. 아래를 보자</p>
<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">ErrorResponse</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="nc">List</span><span class="o">&lt;</span><span class="nc">FieldError</span><span class="o">&gt;</span> <span class="n">fieldErrors</span><span class="o">;</span> 
    <span class="kd">private</span> <span class="nc">List</span><span class="o">&lt;</span><span class="nc">ConstraintViolationError</span><span class="o">&gt;</span> <span class="n">violationErrors</span><span class="o">;</span>  

	<span class="mi">1</span><span class="o">)</span>	
    <span class="kd">private</span> <span class="nf">ErrorResponse</span><span class="o">(</span><span class="kd">final</span> <span class="nc">List</span><span class="o">&lt;</span><span class="nc">FieldError</span><span class="o">&gt;</span> <span class="n">fieldErrors</span><span class="o">,</span>
                          <span class="kd">final</span> <span class="nc">List</span><span class="o">&lt;</span><span class="nc">ConstraintViolationError</span><span class="o">&gt;</span> <span class="n">violationErrors</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">fieldErrors</span> <span class="o">=</span> <span class="n">fieldErrors</span><span class="o">;</span>
        <span class="k">this</span><span class="o">.</span><span class="na">violationErrors</span> <span class="o">=</span> <span class="n">violationErrors</span><span class="o">;</span>
    <span class="o">}</span>

	<span class="mi">2</span><span class="o">)</span>
    <span class="kd">public</span> <span class="kd">static</span> <span class="nc">ErrorResponse</span> <span class="nf">of</span><span class="o">(</span><span class="nc">BindingResult</span> <span class="n">bindingResult</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">return</span> <span class="k">new</span> <span class="nf">ErrorResponse</span><span class="o">(</span><span class="nc">FieldError</span><span class="o">.</span><span class="na">of</span><span class="o">(</span><span class="n">bindingResult</span><span class="o">),</span> <span class="kc">null</span><span class="o">);</span>
    <span class="o">}</span>

		
    <span class="kd">public</span> <span class="kd">static</span> <span class="nc">ErrorResponse</span> <span class="nf">of</span><span class="o">(</span><span class="nc">Set</span><span class="o">&lt;</span><span class="nc">ConstraintViolation</span><span class="o">&lt;?&gt;&gt;</span> <span class="n">violations</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">return</span> <span class="k">new</span> <span class="nf">ErrorResponse</span><span class="o">(</span><span class="kc">null</span><span class="o">,</span> <span class="nc">ConstraintViolationError</span><span class="o">.</span><span class="na">of</span><span class="o">(</span><span class="n">violations</span><span class="o">));</span>
    <span class="o">}</span>

    <span class="mi">3</span><span class="o">)</span>
    <span class="nd">@Getter</span>
    <span class="kd">public</span> <span class="kd">static</span> <span class="kd">class</span> <span class="nc">FieldError</span> <span class="o">{</span>
        <span class="kd">private</span> <span class="nc">String</span> <span class="n">field</span><span class="o">;</span>
        <span class="kd">private</span> <span class="nc">Object</span> <span class="n">rejectedValue</span><span class="o">;</span>
        <span class="kd">private</span> <span class="nc">String</span> <span class="n">reason</span><span class="o">;</span>

				<span class="kd">private</span> <span class="nf">FieldError</span><span class="o">(</span><span class="nc">String</span> <span class="n">field</span><span class="o">,</span> <span class="nc">Object</span> <span class="n">rejectedValue</span><span class="o">,</span> <span class="nc">String</span> <span class="n">reason</span><span class="o">)</span> <span class="o">{</span>
            <span class="k">this</span><span class="o">.</span><span class="na">field</span> <span class="o">=</span> <span class="n">field</span><span class="o">;</span>
            <span class="k">this</span><span class="o">.</span><span class="na">rejectedValue</span> <span class="o">=</span> <span class="n">rejectedValue</span><span class="o">;</span>
            <span class="k">this</span><span class="o">.</span><span class="na">reason</span> <span class="o">=</span> <span class="n">reason</span><span class="o">;</span>
        <span class="o">}</span>

        <span class="kd">public</span> <span class="kd">static</span> <span class="nc">List</span><span class="o">&lt;</span><span class="nc">FieldError</span><span class="o">&gt;</span> <span class="nf">of</span><span class="o">(</span><span class="nc">BindingResult</span> <span class="n">bindingResult</span><span class="o">)</span> <span class="o">{</span>
            <span class="kd">final</span> <span class="nc">List</span><span class="o">&lt;</span><span class="n">org</span><span class="o">.</span><span class="na">springframework</span><span class="o">.</span><span class="na">validation</span><span class="o">.</span><span class="na">FieldError</span><span class="o">&gt;</span> <span class="n">fieldErrors</span> <span class="o">=</span>
                                                        <span class="n">bindingResult</span><span class="o">.</span><span class="na">getFieldErrors</span><span class="o">();</span>
            <span class="k">return</span> <span class="n">fieldErrors</span><span class="o">.</span><span class="na">stream</span><span class="o">()</span>
                    <span class="o">.</span><span class="na">map</span><span class="o">(</span><span class="n">error</span> <span class="o">-&gt;</span> <span class="k">new</span> <span class="nc">FieldError</span><span class="o">(</span>
                            <span class="n">error</span><span class="o">.</span><span class="na">getField</span><span class="o">(),</span>
                            <span class="n">error</span><span class="o">.</span><span class="na">getRejectedValue</span><span class="o">()</span> <span class="o">==</span> <span class="kc">null</span> <span class="o">?</span>
                                            <span class="s">""</span> <span class="o">:</span> <span class="n">error</span><span class="o">.</span><span class="na">getRejectedValue</span><span class="o">().</span><span class="na">toString</span><span class="o">(),</span>
                            <span class="n">error</span><span class="o">.</span><span class="na">getDefaultMessage</span><span class="o">()))</span>
                    <span class="o">.</span><span class="na">collect</span><span class="o">(</span><span class="nc">Collectors</span><span class="o">.</span><span class="na">toList</span><span class="o">());</span>
        <span class="o">}</span>
    <span class="o">}</span>

    <span class="mi">4</span><span class="o">)</span>
    <span class="nd">@Getter</span>
    <span class="kd">public</span> <span class="kd">static</span> <span class="kd">class</span> <span class="nc">ConstraintViolationError</span> <span class="o">{</span>
        <span class="kd">private</span> <span class="nc">String</span> <span class="n">propertyPath</span><span class="o">;</span>
        <span class="kd">private</span> <span class="nc">Object</span> <span class="n">rejectedValue</span><span class="o">;</span>
        <span class="kd">private</span> <span class="nc">String</span> <span class="n">reason</span><span class="o">;</span>

				<span class="kd">private</span> <span class="nf">ConstraintViolationError</span><span class="o">(</span><span class="nc">String</span> <span class="n">propertyPath</span><span class="o">,</span> <span class="nc">Object</span> <span class="n">rejectedValue</span><span class="o">,</span>
                                   <span class="nc">String</span> <span class="n">reason</span><span class="o">)</span> <span class="o">{</span>
            <span class="k">this</span><span class="o">.</span><span class="na">propertyPath</span> <span class="o">=</span> <span class="n">propertyPath</span><span class="o">;</span>
            <span class="k">this</span><span class="o">.</span><span class="na">rejectedValue</span> <span class="o">=</span> <span class="n">rejectedValue</span><span class="o">;</span>
            <span class="k">this</span><span class="o">.</span><span class="na">reason</span> <span class="o">=</span> <span class="n">reason</span><span class="o">;</span>
        <span class="o">}</span>

        <span class="kd">public</span> <span class="kd">static</span> <span class="nc">List</span><span class="o">&lt;</span><span class="nc">ConstraintViolationError</span><span class="o">&gt;</span> <span class="nf">of</span><span class="o">(</span>
                <span class="nc">Set</span><span class="o">&lt;</span><span class="nc">ConstraintViolation</span><span class="o">&lt;?&gt;&gt;</span> <span class="n">constraintViolations</span><span class="o">)</span> <span class="o">{</span>
            <span class="k">return</span> <span class="n">constraintViolations</span><span class="o">.</span><span class="na">stream</span><span class="o">()</span>
                    <span class="o">.</span><span class="na">map</span><span class="o">(</span><span class="n">constraintViolation</span> <span class="o">-&gt;</span> <span class="k">new</span> <span class="nc">ConstraintViolationError</span><span class="o">(</span>
                            <span class="n">constraintViolation</span><span class="o">.</span><span class="na">getPropertyPath</span><span class="o">().</span><span class="na">toString</span><span class="o">(),</span>
                            <span class="n">constraintViolation</span><span class="o">.</span><span class="na">getInvalidValue</span><span class="o">().</span><span class="na">toString</span><span class="o">(),</span>
                            <span class="n">constraintViolation</span><span class="o">.</span><span class="na">getMessage</span><span class="o">()</span>
                    <span class="o">)).</span><span class="na">collect</span><span class="o">(</span><span class="nc">Collectors</span><span class="o">.</span><span class="na">toList</span><span class="o">());</span>
        <span class="o">}</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>1번을 보면 생성자인데 private이 붙었다. 그 소리는 ErrorResponse의 외부에서는 생성자를 호출 못한다는 뜻이 된다. 도대체 왜??</p>

<p>바로 밑의 2번을 보자. 정적 메서드를 오버로딩하고 있다. 그리고 오버로딩한 메서드는 각각 다른 파라미터를 전달 받으며, 메서드 안에서 비로소 생성자를 호출하고 있다.</p>

<p>즉,</p>

<p>of 메서드의 파라미터만 다르게 호출한다면, 필요한 멤버만 전달한 ErrorResponse 인스턴스를 만들 수 있다는 것이다.</p>

<p>그리고 다시</p>

<p>2번 메서드의 반환값을 보면 new ErrorResponse(FieldError.of(bindingResult))이다. 그러면 3번을 가보자.</p>

<p>3번의 경우는 이전 컨트롤러 수준에서 작성했던 핸들러 메서드와 매우 유사하다.</p>

<p>맞다</p>

<p>FieldError멤버 클래스는</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1) 담고자 하는 메시지를 담당하는 멤버변수
2) 멤버 변수를 가공하는 구체적인 로직 of 가 답겨있다.
</code></pre></div></div>]]></content><author><name>Shinjae Kim</name><email>lo97729@gmail.com</email></author><category term="코드스테이츠" /><category term="Spring" /><summary type="html"><![CDATA[[week10] 간단정리 @ExceptionHandler를 이용하여 예외를 처리하자. @RestControllerAdvice를 이용하여 예외처리를 통합하자.(중복을 제거하자) 예외의 가공을 담당하는 클래스를 만들자 (여기서는 ErrorResponse)]]></summary></entry><entry><title type="html">Section 3 Spring - Service, Mapper</title><link href="http://localhost:4000/%EC%BD%94%EB%93%9C%EC%8A%A4%ED%85%8C%EC%9D%B4%EC%B8%A0/Section-3-SERVICE-MAPPER/" rel="alternate" type="text/html" title="Section 3 Spring - Service, Mapper" /><published>2022-08-23T00:00:00+09:00</published><updated>2022-08-23T00:00:00+09:00</updated><id>http://localhost:4000/%EC%BD%94%EB%93%9C%EC%8A%A4%ED%85%8C%EC%9D%B4%EC%B8%A0/Section%203%20SERVICE%20MAPPER</id><content type="html" xml:base="http://localhost:4000/%EC%BD%94%EB%93%9C%EC%8A%A4%ED%85%8C%EC%9D%B4%EC%B8%A0/Section-3-SERVICE-MAPPER/"><![CDATA[<p>[week10]</p>
<h2 id="간단정리">간단정리</h2>
<ul>
  <li>DTO클래스와 mapper를 통해 Entity를 만든다.</li>
  <li>@Service 애너테이션은 해당 클래스가 서비스 계층임을 알려준다.</li>
  <li>Controller에 Service와 mapper를 DI한다.</li>
</ul>

<p><br /></p>

<h2 id="느낀점">느낀점</h2>
<p>이해가 잘 되지 않았던 DI의 개념이 쓰다보니 이해가 된다. 그리고 controller와 dto, entity와 service를 잘 다져놓아야 추후 진행할 data acess또한 이해가 잘 될 것 같다. 더 열심히 하자.</p>

<p><br /></p>

<h2 id="스스로-답해보기">스스로 답해보기</h2>
<ul>
  <li>Entity는 무엇이며 왜 만드는가?</li>
  <li>Service 클래스를 만들 수 있는가?</li>
  <li>Controller에 적용 가능한가?</li>
</ul>

<p><br /></p>

<h2 id="정리---들어가며">정리 - 들어가며</h2>

<p>WAS(Web Application Server)를 보면,</p>

<p>클라이언트로부터 도착한 요청이 어떤 흐름으로 진행되는가를 파악할 수 있다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>API계층 -&gt; Service계층 -&gt; DataAccess계층
</code></pre></div></div>

<p>지금까지는 API계층이 무엇인지, 그리고 Controller의 역할과 Dto클래스에 대해서 알아보았다.</p>

<p>이제 Service로 넘어가자.</p>

<p>정리하기에 앞서 지금까지 만들었던 코드를 조금 다듬고 가야겠다는 생각이 든다.</p>

<p><em>(컨트롤러)</em></p>
<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@RestController</span>
<span class="nd">@RequestMapping</span><span class="o">(</span><span class="s">"/mylist/practice"</span><span class="o">)</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">ControllerExample</span><span class="o">{</span>
    
    <span class="nd">@PostMapping</span>
    <span class="kd">public</span> <span class="nc">ResponseEntity</span> <span class="nf">post</span><span class="o">(</span><span class="nd">@RequestBody</span> <span class="nc">PostDto</span> <span class="n">postDto</span><span class="o">){</span>
        

        <span class="k">return</span> <span class="nc">ResponseEntity</span><span class="o">&lt;&gt;(</span><span class="n">postDto</span><span class="o">,</span> <span class="nc">HttpStatus</span><span class="o">.</span><span class="na">CREATED</span><span class="o">);</span>
    <span class="o">}</span>
    
<span class="o">}</span>
</code></pre></div></div>

<p><em>(DTO)</em></p>
<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">PostDto</span><span class="o">{</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="k">this</span><span class="o">;</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">name</span><span class="o">;</span>
    <span class="kd">private</span> <span class="kt">int</span> <span class="n">age</span><span class="o">;</span>

    <span class="kd">public</span> <span class="nc">String</span> <span class="nf">getThis</span><span class="o">(){</span>
        <span class="k">return</span> <span class="k">this</span><span class="o">;</span>
    <span class="o">}</span>
    
    <span class="kd">public</span> <span class="nc">String</span> <span class="nf">name</span><span class="o">(){</span>
        <span class="k">return</span> <span class="n">name</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="kt">int</span> <span class="nf">age</span><span class="o">(){</span>
        <span class="k">return</span> <span class="n">age</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>단순하게 this, name, age를 클라이언트로부터 전달받는 responsebody와 매치시켰는데,</p>

<p>지금부터 이 Post는 졸업생들의 직업을 조사하는 기능중의 일부로 판단하여</p>

<p>this를 job으로 바꾸어 생각하고 진행하겠다.</p>

<p><br /></p>

<h3 id="1-엔티티">1. 엔티티</h3>

<p>WAS상의 계층들은 각 계층의 ‘주’역할만을 담당하는 것이 코드분석, 유지보수에 유리하게 작용할 것이다.</p>

<p>이러한 이유로 Controller 클래스가 클라이언트와의 정보공유만을 위해 작동하도록 responsebody와의 변환을 담당하는 dto클래스를 따로 만들어 주었다.</p>

<p>이와 마찬가지의 이유로 서비스 클래스에서도 엔티티라는 것이 존재한다.</p>

<p><strong><em>엔티티는 비즈니스 로직을 처리하기 위한, 처리한 데이터를 전달하기 위핸 객체이다.</em></strong></p>

<p><em>(Entity의 구현)</em></p>
<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Data</span>
<span class="nd">@NoArgsConstructor</span>
<span class="nd">@AllArgsConstructor</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">Entity</span><span class="o">{</span>
    <span class="kd">private</span> <span class="kt">long</span> <span class="nc">Id</span><span class="o">;</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">job</span><span class="o">;</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">name</span><span class="o">;</span>
    <span class="kd">private</span> <span class="kt">int</span> <span class="n">age</span><span class="o">;</span>
<span class="o">}</span>
</code></pre></div></div>

<p>바뀐점이라면 Lombok을 이용한 애너테이션으로 게터,세터(@Data) 메서드와 생성자 코드를 설정해주었다.</p>

<p>그리고 long형으로 Id를 선언하였다. 이는 auto increment식별자로서 추후 데이터액세스 계층에서 활약할 예정이다.</p>

<p><br /></p>

<h3 id="2-mapper의-등장">2. Mapper의 등장</h3>

<p>앞선 DTO는 @RequestBody 애너테이션으로 JSON -&gt; 객체 의 자동 변환이 되었다.</p>

<p>마찬가지로 DTO또한 엔티티로 변환해 주어야 할 것인데,</p>

<p>앞서 언급했듯 서비스 계층에서는 비즈니스 로직만을 담당하는 것이 옳을 것이다.</p>

<p>따라서 DTO -&gt; Entity의 역할을 담당하는 클래스를 따로 만들어 주어야 한다.</p>

<p>이 역할을 담당하는 것이 Mapper이다.</p>

<p>방법은 여러가지다.</p>

<ol>
  <li>직접 하나하나 일치시킨다.</li>
</ol>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">Mapper</span><span class="o">{</span>
    <span class="kd">private</span> <span class="kt">long</span> <span class="nc">Id</span><span class="o">;</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">job</span><span class="o">;</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">name</span><span class="o">;</span>
    <span class="kd">private</span> <span class="kt">int</span> <span class="n">age</span><span class="o">;</span>

    <span class="kd">public</span> <span class="nc">Entity</span> <span class="nf">postDtoToMapper</span><span class="o">(</span><span class="nc">PostDto</span> <span class="n">postDto</span><span class="o">){</span>
        <span class="k">return</span> <span class="k">new</span> <span class="nf">Entity</span><span class="o">(</span><span class="mi">0L</span><span class="o">,</span>
                        <span class="nc">PostDto</span><span class="o">.</span><span class="na">getJob</span><span class="o">(),</span> 
                        <span class="nc">PostDto</span><span class="o">.</span><span class="na">getName</span><span class="o">(),</span> 
                        <span class="nc">PostDto</span><span class="o">.</span><span class="na">getAge</span><span class="o">());</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="nc">ResponseDto</span> <span class="nf">EntityToResponseDto</span><span class="o">(</span><span class="nc">Entity</span> <span class="n">entity</span><span class="o">){</span>
        <span class="k">return</span> <span class="k">new</span> <span class="nf">ResponseDto</span><span class="o">(</span><span class="nc">Entity</span><span class="o">.</span><span class="na">getId</span><span class="o">(),</span>
                            <span class="nc">Entity</span><span class="o">.</span><span class="na">getJob</span><span class="o">(),</span>
                            <span class="nc">Entity</span><span class="o">.</span><span class="na">getName</span><span class="o">(),</span>
                            <span class="nc">Entity</span><span class="o">.</span><span class="na">getAge</span><span class="o">());</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<ol>
  <li>MapStruct을 이용한다.</li>
</ol>

<p>1의 경우처럼 하나하나 매핑해주는 것은 개발자의 몫일 것인데, 매핑할 것이 많아지면 일이 많아진다.</p>

<p>따라서 MapStruct 인터페이스를 이용할 수 있다.</p>

<p>일단</p>

<p><em>(build.gradle에 의존성 라이브러리 추가)</em></p>
<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">dependencies</span> <span class="o">{</span>
	<span class="o">...</span>
	<span class="o">...</span>
	<span class="n">implementation</span> <span class="err">'</span><span class="n">org</span><span class="o">.</span><span class="na">mapstruct</span><span class="o">:</span><span class="nl">mapstruct:</span><span class="mf">1.4</span><span class="o">.</span><span class="mi">2</span><span class="o">.</span><span class="na">Final</span><span class="err">'</span>
	<span class="n">annotationProcessor</span> <span class="err">'</span><span class="n">org</span><span class="o">.</span><span class="na">mapstruct</span><span class="o">:</span><span class="n">mapstruct</span><span class="o">-</span><span class="nl">processor:</span><span class="mf">1.4</span><span class="o">.</span><span class="mi">2</span><span class="o">.</span><span class="na">Final</span><span class="err">'</span>
<span class="o">}</span>
</code></pre></div></div>
<p><em>(인터페이스 작성)</em></p>
<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Mapper</span><span class="o">(</span><span class="n">componentModel</span> <span class="o">=</span> <span class="s">"spring"</span><span class="o">)</span>
<span class="kd">public</span> <span class="kd">interface</span> <span class="nc">Mapper</span><span class="o">{</span>
    <span class="nc">Entity</span> <span class="nf">postDtoToMapper</span><span class="o">(</span><span class="nc">PostDto</span> <span class="n">postDto</span><span class="o">);</span>
    <span class="nc">ResponseDto</span> <span class="nf">EntityToResponseDto</span><span class="o">(</span><span class="nc">Entity</span> <span class="n">entity</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div>

<p>그리고 responseDto클래스를 적성한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Data</span>
<span class="nd">@NoArgsConstructor</span>
<span class="nd">@AllArgsConstructor</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">ResponseDto</span><span class="o">{</span>
    <span class="kd">private</span> <span class="kt">long</span> <span class="nc">Id</span><span class="o">;</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">job</span><span class="o">;</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">name</span><span class="o">;</span>
    <span class="kd">private</span> <span class="kt">int</span> <span class="n">age</span><span class="o">;</span>
<span class="o">}</span>
</code></pre></div></div>

<p>Mapper클래스에서는 각각의 핸들러 메서드에 맞게 사용 가능하도록</p>

<p>1) DTO -&gt; Entity의 기능을 담다하는 메서드와</p>

<p>2) Entity -&gt; DTO의 기능을 담당하는 메서드를 작성해주었다.</p>

<p>또한 2)를 위해서 ResponseDto클래스를 작성해주었다. 이 클래스의 존재는 필수적이다. 비즈니스로직을 거쳐 Entity클래스에 담긴 정보를 클라이언트에게 넘겨주기 위해서는 다시 DTO클래스로의 변환이 필요하기 때문이다.</p>

<p><br /></p>

<h3 id="3-service-계층">3. Service 계층</h3>

<p>일단 위의 재료들을 바탕으로 작성해보도록 하겠다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Service</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nf">ServiceExample</span><span class="o">(){</span>

    <span class="kd">public</span> <span class="nc">Entity</span> <span class="nf">CreatePost</span><span class="o">(</span><span class="nc">Entity</span> <span class="n">entity</span><span class="o">){</span>
        <span class="c1">// Business logic</span>
        <span class="nc">Entity</span> <span class="n">createEntity</span> <span class="o">=</span> <span class="n">entity</span>

        <span class="k">return</span> <span class="n">entity</span> 
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>일단 @Service라는 애너테이션은 해당 클래스가 ‘비즈니스로직을 실행함’을 알리고 ‘Bean’으로 등록해준다.</p>

<p>그리고 아직 비즈니스 로직은 작성이 안되어 파라미터로 받은 엔티티를 그대로 반환하지만 액세스 계층까지 만든다면 비즈니스 로직이 들어가게 될 것이다.</p>

<p><br /></p>

<h3 id="4-controller에-service-di">4. controller에 service DI</h3>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@RestController</span>
<span class="nd">@RequestMapping</span><span class="o">(</span><span class="s">"/mylist/practice"</span><span class="o">)</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">ControllerExample</span><span class="o">{</span>

    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">ServiceExample</span> <span class="n">service</span><span class="o">;</span>
    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">Mapper</span> <span class="n">mapper</span><span class="o">;</span>

    <span class="kd">public</span> <span class="nf">ControllerExample</span><span class="o">(</span><span class="nc">ServiceExample</span> <span class="n">service</span><span class="o">,</span> <span class="nc">Mapper</span> <span class="n">mapper</span><span class="o">){</span>
        <span class="k">this</span><span class="o">.</span><span class="na">service</span> <span class="o">=</span> <span class="n">service</span><span class="o">;</span>
        <span class="k">this</span><span class="o">.</span><span class="na">mapper</span> <span class="o">=</span> <span class="n">mapper</span><span class="o">;</span>
    <span class="o">}</span>
    
     <span class="nd">@PostMapping</span>
    <span class="kd">public</span> <span class="nc">ResponseEntity</span> <span class="nf">post</span><span class="o">(</span><span class="nd">@RequestBody</span> <span class="nc">PostDto</span> <span class="n">postDto</span><span class="o">){</span>
        
        <span class="nc">Entity</span> <span class="n">response</span> <span class="o">=</span> <span class="n">service</span><span class="o">.</span><span class="na">createPost</span><span class="o">(</span><span class="n">mapper</span><span class="o">.</span><span class="na">postDtoToEntity</span><span class="o">(</span><span class="n">postDto</span><span class="o">));</span>

        <span class="k">return</span> <span class="nc">ResponseEntity</span><span class="o">&lt;&gt;(</span><span class="n">mapper</span><span class="o">.</span><span class="na">EntityToResponseDto</span><span class="o">(</span><span class="n">response</span><span class="o">),</span> <span class="nc">HttpStatus</span><span class="o">.</span><span class="na">CREATED</span><span class="o">);</span>
    <span class="o">}</span>
    
<span class="o">}</span>
</code></pre></div></div>

<p>DI를 이용하여 service와 mapper를 선언해주고 post핸들러의 내용을 추가시켰다.</p>]]></content><author><name>Shinjae Kim</name><email>lo97729@gmail.com</email></author><category term="코드스테이츠" /><category term="Spring" /><summary type="html"><![CDATA[[week10] 간단정리 DTO클래스와 mapper를 통해 Entity를 만든다. @Service 애너테이션은 해당 클래스가 서비스 계층임을 알려준다. Controller에 Service와 mapper를 DI한다.]]></summary></entry><entry><title type="html">Section 3 Spring MVC - DTO, 유효성 검사</title><link href="http://localhost:4000/%EC%BD%94%EB%93%9C%EC%8A%A4%ED%85%8C%EC%9D%B4%EC%B8%A0/Section-3-DTO/" rel="alternate" type="text/html" title="Section 3 Spring MVC - DTO, 유효성 검사" /><published>2022-08-22T00:00:00+09:00</published><updated>2022-08-22T00:00:00+09:00</updated><id>http://localhost:4000/%EC%BD%94%EB%93%9C%EC%8A%A4%ED%85%8C%EC%9D%B4%EC%B8%A0/Section%203%20DTO</id><content type="html" xml:base="http://localhost:4000/%EC%BD%94%EB%93%9C%EC%8A%A4%ED%85%8C%EC%9D%B4%EC%B8%A0/Section-3-DTO/"><![CDATA[<p>[week10]</p>
<h2 id="간단정리">간단정리</h2>
<p>컨트롤러의 핸들러 메서드는 클라이언트와의 데이터 전송을 담당하는 계층으로써의 역할을 다해야 한다. 때문에 dto를 만들고, 유효성 검사를 진행하게 한다면 그 기능에 충실하게 해줄 수 있다.</p>

<p><br /></p>

<h2 id="느낀점">느낀점</h2>
<p>점점 느낀점 적는 것이 무의미해지고 있다. 매일 새롭고 매주 ‘엥??’ 이러는데, 그냥 정말 이게 다라서 뭐 추가할 말이 있나 싶다 라는 생각이 나를 지배한다. 느낀점 파트에 다가오면?? 말도 한국식 어순으로 안쓰고 있는 뭔가 그렇다고 영어도 아닌, 생각이 매우 혼재되어 있다.</p>

<p><br /></p>

<h2 id="스스로-답해보기">스스로 답해보기</h2>
<ul>
  <li>dto는 무엇의 약자인가??</li>
  <li>dto는 왜 사용하는가??</li>
  <li>dto 적용 흐름</li>
  <li>@requestbody와 @responsebody 애너테이션</li>
</ul>

<p><br /></p>

<h2 id="정리---들어가며">정리 - 들어가며</h2>

<p>DTO는 왜 사용할까??
현재 리소스의 개별 데이터들의 개수가 적기 때문에, 바인딩을 하는데에 있어 @Requestparam을 이용해도 커버가 가능하다.
하지만 리소스에 포함되는 데이터의 개수들이 많아진다묜?? 코드가 복잡해지고 이는 즉 유지보수의 어려움으로 직결될 것이다.</p>

<p>따라서 DTO로 받아서 해결하자</p>

<h3 id="dto">DTO</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>DTO - Data Transfer Object

    데이터를 전송하기 위해 사용하는 객체
</code></pre></div></div>

<p>잠시 흐름을 짚고 가도록 하겠다.</p>

<p>이 전 컨트롤러에서 Http message의 ResponseBody의 데이터들을 하나하나 @RequestParam으로 받아왔었다.</p>

<p>이 경우 핸들러 메서드에서는 <strong><em>‘클라이언트와의 통신만은 담당한다’</em></strong> 라는 설계원칙을 달성하기 힘들어진다.</p>

<p>핸들러 메서드 안에서 데이터들로 Map을 채워준 후 ResponseEntity에 전달해주어야 하기 때문이다.</p>

<p>하지만 <strong><em>DTO</em></strong>를 이용하면 이 문제점을 해결할 수 있다.</p>

<h3 id="적용">적용</h3>

<p>DTO 객체를 이용하는 방법은 간단하다.</p>

<p>DTO 클래스를 따로 설계한 후 <strong><em>멤버 변수로 @RequestParam 애너테이션으로 받아오던 변수들을 선언한다.</em></strong></p>

<p>만약 컨트롤러의 post핸들러 메서드가 다음과 같았다면</p>

<p><em>(수정전)</em></p>
<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@RestController</span>
<span class="nd">@RequestMapping</span><span class="o">(</span><span class="s">"/mylist/practice"</span><span class="o">)</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">ControllerExample</span><span class="o">{</span>
    
    <span class="nd">@PostMapping</span>
    <span class="kd">public</span> <span class="nc">ResponseEntity</span> <span class="nf">post</span><span class="o">(</span><span class="nd">@RequestParam</span><span class="o">(</span><span class="s">"this"</span><span class="o">)</span> <span class="nc">String</span> <span class="k">this</span><span class="o">,</span>
                               <span class="nd">@RequestParam</span><span class="o">(</span><span class="s">"name"</span><span class="o">)</span> <span class="nc">String</span> <span class="n">name</span><span class="o">,</span>
                               <span class="nd">@RequestParam</span><span class="o">(</span><span class="s">"age"</span><span class="o">)</span> <span class="kt">int</span> <span class="n">age</span><span class="o">){</span>
        <span class="nc">Map</span><span class="o">&lt;</span><span class="nc">Object</span><span class="o">,</span> <span class="nc">Object</span><span class="o">&gt;</span> <span class="n">list</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">HashMap</span><span class="o">&lt;&gt;();</span>

        <span class="n">map</span><span class="o">.</span><span class="na">put</span><span class="o">(</span><span class="s">"this"</span><span class="o">,</span> <span class="k">this</span><span class="o">);</span>
        <span class="n">map</span><span class="o">.</span><span class="na">put</span><span class="o">(</span><span class="s">"name"</span><span class="o">,</span> <span class="n">name</span><span class="o">);</span>
        <span class="n">map</span><span class="o">.</span><span class="na">put</span><span class="o">(</span><span class="s">"age"</span><span class="o">,</span> <span class="n">age</span><span class="o">);</span>

        <span class="k">return</span> <span class="nc">ResponseEntity</span><span class="o">&lt;</span><span class="nc">Map</span><span class="o">&gt;(</span><span class="n">list</span><span class="o">,</span> <span class="nc">HttpStatus</span><span class="o">.</span><span class="na">CREATED</span><span class="o">);</span>
    <span class="o">}</span>
    
<span class="o">}</span>
</code></pre></div></div>

<p>이렇게 DTO 클래스를 만들어주자.</p>

<p><em>(DTO 클래스 설계)</em></p>
<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">PostDto</span><span class="o">{</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="k">this</span><span class="o">;</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">name</span><span class="o">;</span>
    <span class="kd">private</span> <span class="kt">int</span> <span class="n">age</span><span class="o">;</span>

    <span class="kd">public</span> <span class="nc">String</span> <span class="nf">getThis</span><span class="o">(){</span>
        <span class="k">return</span> <span class="k">this</span><span class="o">;</span>
    <span class="o">}</span>
    
    <span class="kd">public</span> <span class="nc">String</span> <span class="nf">name</span><span class="o">(){</span>
        <span class="k">return</span> <span class="n">name</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="kt">int</span> <span class="nf">age</span><span class="o">(){</span>
        <span class="k">return</span> <span class="n">age</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>그리고 이제 Dto 클래스를 넣어주자.</p>

<p><em>(적용)</em></p>
<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@RestController</span>
<span class="nd">@RequestMapping</span><span class="o">(</span><span class="s">"/mylist/practice"</span><span class="o">)</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">ControllerExample</span><span class="o">{</span>
    
    <span class="nd">@PostMapping</span>
    <span class="kd">public</span> <span class="nc">ResponseEntity</span> <span class="nf">post</span><span class="o">(</span><span class="nd">@RequestBody</span> <span class="nc">PostDto</span> <span class="n">postDto</span><span class="o">){</span>
        

        <span class="k">return</span> <span class="nc">ResponseEntity</span><span class="o">&lt;&gt;(</span><span class="n">postDto</span><span class="o">,</span> <span class="nc">HttpStatus</span><span class="o">.</span><span class="na">CREATED</span><span class="o">);</span>
    <span class="o">}</span>
    
<span class="o">}</span>
</code></pre></div></div>

<p>한결 간단해졌다.</p>

<p>여기서 중요한 점은 @RequestBody를 추가했다는 것이다.</p>

<p>이렇게하면 클라이언트쪽에서 http메시지가 JSON형식으로 전송될때 자동으로 지정한 DTO객체로 변환해준다.</p>

<h3 id="유효성-검사">유효성 검사</h3>

<p>간결함에 더하여 DTO에 유효성 검사를 추가함으로써 잘못된 형식의 입력들을 방지할 수 있다.</p>

<p>프론트엔드쪽에서도 유효성검사를 진행하지만 breakpoint후 데이터를 바꾸는 것이 가능하기때문에 한번 더 해주어야 하는 것이다.</p>

<p>방법은 간단하게 DTO클래스의 멤버변수에 애너테이션을 적용해주는 것인데,</p>

<p>애너테이션들과 정규식표현, 그리고 커스텀 애너테이션을 설정하는 것은 따로 정리를 해서 링크를 걸도록 하겠다.(0822)</p>]]></content><author><name>Shinjae Kim</name><email>lo97729@gmail.com</email></author><category term="코드스테이츠" /><category term="Spring" /><summary type="html"><![CDATA[[week10] 간단정리 컨트롤러의 핸들러 메서드는 클라이언트와의 데이터 전송을 담당하는 계층으로써의 역할을 다해야 한다. 때문에 dto를 만들고, 유효성 검사를 진행하게 한다면 그 기능에 충실하게 해줄 수 있다.]]></summary></entry></feed>